---
phase: 02-content-layout
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/components/Hero/Hero.tsx
  - src/components/Features/Features.tsx
  - src/components/HowItWorks/HowItWorks.tsx
  - src/components/DataFlow/DataFlow.tsx
  - src/components/OpenSource/OpenSource.tsx
  - src/components/Community/Community.tsx
  - src/components/Footer/Footer.tsx
autonomous: false
requirements: [PERF-02]

must_haves:
  truths:
    - "Page layout works correctly on 320px mobile width"
    - "Page layout works correctly on 768px tablet width"
    - "Page layout works correctly on 1280px desktop width"
    - "All interactive elements (links, buttons, badges) have minimum 44x44px touch targets"
    - "Feature cards stack to 1 column on mobile and show 2x2 on desktop"
    - "How it works steps stack vertically on mobile with arrows hidden"
  artifacts:
    - path: "src/components/Features/Features.tsx"
      provides: "Responsive feature grid"
      contains: "grid-cols-1"
    - path: "src/components/HowItWorks/HowItWorks.tsx"
      provides: "Responsive step layout"
      contains: "flex-col"
  key_links:
    - from: "All interactive elements"
      to: "PERF-02 requirement"
      via: "min-h-11 or equivalent 44px touch target"
      pattern: "min-h-11|min-w-11|h-11|w-11|py-3.*px-8"
---

<objective>
Audit and fix responsive layout across all breakpoints and verify touch target compliance.

Purpose: Ensure the complete page works flawlessly on mobile (320px) through desktop (1280px+) with proper touch targets for all interactive elements. This is the quality gate before Phase 2 is complete.

Output: Responsive fixes applied + human verification of visual quality.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-layout/02-CONTEXT.md
@.planning/phases/02-content-layout/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive audit and touch target fixes</name>
  <files>src/components/Hero/Hero.tsx, src/components/Features/Features.tsx, src/components/HowItWorks/HowItWorks.tsx, src/components/DataFlow/DataFlow.tsx, src/components/OpenSource/OpenSource.tsx, src/components/Community/Community.tsx, src/components/Footer/Footer.tsx</files>
  <action>
Run `npm run dev` and systematically audit each section at 3 breakpoints: 320px, 768px, 1280px.

Check and fix these specific items:

**320px (small mobile) audit:**
- Hero: headline text size not overflowing, Coming Soon badge fits, waveform placeholder doesn't obscure text
- Features: cards stack to 1 column, card content doesn't overflow
- HowItWorks: steps stack vertically, arrows hidden, icons and text centered
- DataFlow: card padding reduced if needed (p-6 instead of p-8 on mobile), diagram elements stack or wrap
- OpenSource: text doesn't overflow, GitHub link touch target adequate
- Community: headline wraps gracefully, Telegram button doesn't overflow
- Footer: flex wraps if needed on very small screens, icons maintain 44px targets

**768px (tablet) audit:**
- Features: 2-column grid works
- HowItWorks: horizontal layout starts working (or still stacked -- verify md breakpoint)
- DataFlow: card has good proportions
- General: containers aren't too wide or too narrow

**1280px (desktop) audit:**
- max-w-6xl containers center correctly with adequate side padding
- Feature 2x2 grid has proper spacing
- HowItWorks horizontal steps with arrows display correctly
- Overall spacing feels premium (py-28 adequate)

**Touch targets (PERF-02):**
Audit ALL interactive elements across all sections:
- Coming Soon badge: not interactive, but if it becomes a link later, padding should be adequate
- GitHub link in OpenSource: must have min 44x44px touch area
- Telegram button in Community: already has py-3 px-8 which exceeds 44px
- Footer GitHub icon: must have h-11 w-11 (44px)
- Footer Telegram icon: must have h-11 w-11 (44px)
- Language toggle in Nav: verify it already meets 44px (from Phase 1)

For any element below 44px, add `min-h-11 min-w-11` or increase padding.

**Text overflow safety:**
- Add `break-words` or `overflow-wrap: break-word` to long text containers if needed
- Verify headline doesn't cause horizontal scroll on 320px

After fixes, run `npm run build && npm run lint` to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>All sections render correctly at 320px, 768px, and 1280px. All interactive elements have minimum 44x44px touch targets. No horizontal overflow on any breakpoint. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and responsive verification</name>
  <files>N/A</files>
  <action>
Human verifies the complete Phase 2 landing page visually.

What was built: Complete landing page with all 7 sections -- Hero (full viewport, headline, Coming Soon badge), Features (4 glassmorphism cards), How It Works (3 steps), Data Flow (diagram card), Open Source (GitHub link), Community (Telegram CTA), Footer (credits, icons, privacy statement). Responsive across mobile/tablet/desktop. Bilingual FR/EN.

How to verify:
1. Run `npm run dev` and open http://localhost:3000/fr
2. Scroll the entire page top to bottom -- verify all 7 sections are present in order
3. Check Hero: full viewport, bold headline, Coming Soon badge, ambient waveform behind text
4. Check Features: 4 glassmorphism cards with frosted glass effect
5. Check How It Works: 3 steps with arrow connectors
6. Check Data Flow: glassmorphism card with voice-to-text diagram
7. Check Open Source: GitHub link present
8. Check Community: prominent Telegram CTA button
9. Check Footer: PIVI Solutions credit, GitHub/Telegram icons, privacy statement
10. Toggle to EN (language toggle in nav) -- verify all text switches
11. Open DevTools, toggle to mobile view (375px width) -- verify responsive layout
12. Check that feature cards stack to 1 column on mobile
13. Verify the overall premium dark aesthetic matches the brand

Resume signal: Type "approved" or describe any issues to fix.
  </action>
  <verify>Human approval received</verify>
  <done>Human confirms the complete page looks correct, is responsive, bilingual, and matches the premium dark brand aesthetic.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Page renders correctly at 320px, 768px, 1280px widths
- All interactive elements pass 44px touch target minimum
- No horizontal scroll on any breakpoint
- Human confirms visual quality and bilingual content
</verification>

<success_criteria>
- Page is fully responsive from 320px mobile to desktop
- All touch targets meet 44px minimum (PERF-02)
- Human approves the complete visual experience
- Both /fr and /en render all sections with correct translations
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-layout/02-03-SUMMARY.md`
</output>
