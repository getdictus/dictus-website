---
phase: 07-content-cta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Comparison/Comparison.tsx
  - src/components/Comparison/ComparisonTable.tsx
  - src/components/Comparison/ComparisonCards.tsx
  - src/messages/en.json
  - src/messages/fr.json
  - src/app/[locale]/page.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07]

must_haves:
  truths:
    - "User can see a comparison table with Dictus + 4 competitors on desktop"
    - "Table shows 6 feature dimensions with checkmark/cross icons and text"
    - "Dictus column is visually highlighted with accent border and glow"
    - "Header row sticks to top on scroll (below Nav)"
    - "On mobile (<768px), table becomes a card stack with Dictus expanded and others collapsed"
    - "Rows stagger-animate on scroll entry"
    - "Comparison data displays in FR or EN matching site locale"
  artifacts:
    - path: "src/components/Comparison/Comparison.tsx"
      provides: "Main comparison section wrapper"
    - path: "src/components/Comparison/ComparisonTable.tsx"
      provides: "Desktop table with sticky header and accent column"
    - path: "src/components/Comparison/ComparisonCards.tsx"
      provides: "Mobile card stack with expand/collapse"
    - path: "src/messages/en.json"
      provides: "English Comparison namespace"
      contains: "Comparison"
    - path: "src/messages/fr.json"
      provides: "French Comparison namespace"
      contains: "Comparison"
  key_links:
    - from: "src/components/Comparison/Comparison.tsx"
      to: "src/components/Comparison/ComparisonTable.tsx"
      via: "imports and renders at md: breakpoint"
      pattern: "hidden md:block"
    - from: "src/components/Comparison/Comparison.tsx"
      to: "src/components/Comparison/ComparisonCards.tsx"
      via: "imports and renders below md: breakpoint"
      pattern: "md:hidden"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/Comparison/Comparison.tsx"
      via: "import and render between Features and HowItWorks"
      pattern: "ScrollReveal.*Comparison"
---

<objective>
Build the competitor comparison table section showing Dictus vs 4 competitors (Apple Dictation, WhisperFlow, SuperWhisper, MacWhisper) across 6 feature dimensions.

Purpose: Let visitors compare Dictus's advantages at a glance -- factual, neutral, letting checkmarks speak for themselves.
Output: New Comparison section placed after Features, before HowItWorks, with desktop table and mobile card stack.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-cta/07-CONTEXT.md
@.planning/phases/07-content-cta/07-RESEARCH.md

@src/components/shared/ScrollReveal.tsx
@src/components/Features/Features.tsx
@src/app/[locale]/page.tsx
@src/messages/en.json
@src/messages/fr.json

<interfaces>
<!-- Existing patterns the executor needs -->

From src/components/shared/ScrollReveal.tsx:
```typescript
export const staggerChildVariants: Variants = {
  hidden: { opacity: 0, y: 40 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: "easeOut" } },
};

export default function ScrollReveal({ children, className, stagger = false }: Props)
```

From src/app/[locale]/page.tsx (insertion point):
```typescript
// Comparison goes between Features and HowItWorks
<ScrollReveal><Features /></ScrollReveal>
// --> INSERT HERE
<ScrollReveal><HowItWorks /></ScrollReveal>
```

Section layout pattern (from existing sections):
```
<section className="bg-ink py-28">  // or bg-ink-deep, bg-ink-2
  <div className="mx-auto max-w-6xl px-6">
```

Tier 2 glass pattern (from 06-visual-effects):
```
rounded-2xl border border-border bg-[var(--glass-t2-bg)] backdrop-blur-[12px] backdrop-saturate-[1.2]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comparison i18n data + desktop table component</name>
  <files>src/messages/en.json, src/messages/fr.json, src/components/Comparison/ComparisonTable.tsx, src/components/Comparison/Comparison.tsx</files>
  <action>
**Step 1: Add Comparison namespace to i18n files**

Add a `"Comparison"` namespace to both `src/messages/en.json` and `src/messages/fr.json` with:
- `title`: Section heading (e.g., EN: "How Dictus compares", FR: "Comment Dictus se compare")
- Feature dimension labels: `feature_price`, `feature_offline`, `feature_privacy`, `feature_platforms`, `feature_ai_rewrite`, `feature_opensource`
- For each of the 5 products (dictus, apple, whisperflow, superwhisper, macwhisper):
  - `{product}_name`: Display name
  - `{product}_price`: Price text (e.g., "Free", "$4.99/mo", "Built-in", "$29", "$29")
  - `{product}_platforms`: Platform text (e.g., "iOS", "iOS, macOS", "iOS", "macOS", "macOS")
- Boolean features (offline, privacy, ai_rewrite, opensource) do NOT need i18n -- they render as checkmark/cross SVG icons

Competitor data (factual):
| | Dictus | Apple Dictation | WhisperFlow | SuperWhisper | MacWhisper |
|---|---|---|---|---|---|
| Price | Free | Built-in | $4.99/mo | $29 | $29 |
| 100% Offline | yes | no | yes | yes | yes |
| Privacy-first | yes | no | yes | yes | yes |
| Platforms | iOS | iOS, macOS | iOS | macOS | macOS |
| AI Rewrite | yes | no | no | yes | yes |
| Open Source | yes | no | no | no | no |

**Step 2: Create ComparisonTable.tsx (desktop)**

Create `src/components/Comparison/ComparisonTable.tsx` as a `"use client"` component:
- Use `useTranslations("Comparison")` for all text
- Render a semantic `<table>` with `<thead>` and `<tbody>`
- `<thead>` row: empty corner cell + 5 product name columns (`<th scope="col">`)
- Dictus header cell gets accent background: `bg-accent/10 text-accent font-medium`
- Feature rows (`<tr>`) with `<th scope="row">` for dimension label + 5 `<td>` cells
- Boolean cells render CheckIcon (green checkmark) or CrossIcon (white-40 cross) with `<span className="sr-only">` for accessibility
- Text cells (price, platforms) render translated string
- Dictus data column gets left border highlight: `border-l-2 border-accent bg-[rgba(61,126,255,0.12)]` (glow-soft)
- Sticky header: `<thead>` cells get `sticky top-16 z-10 bg-[var(--glass-t2-bg)] backdrop-blur-[12px]` (top-16 = 4rem = 64px Nav height)
- Inline SVG icons for check/cross (no icon library):
  - Check: green path `d="M5 10l3.5 3.5L15 7"` with `text-success`, 20x20
  - Cross: white-40 path `d="M6 6l8 8M14 6l-8 8"` with `text-white-40`, 20x20
- Table container: NO `overflow: hidden` or `overflow: auto` (breaks sticky)
- Use `staggerChildVariants` from ScrollReveal for row animation with `m.tr` elements
- Wrap the tbody in `m.tbody` with stagger container variants (~80ms staggerChildren delay)
- Use `whileInView` / `viewport={{ once: true, amount: 0.2 }}` for scroll-triggered stagger
- Table styling: premium feel with subtle row borders (`border-b border-border`), generous padding (`px-6 py-4` on cells)

**Step 3: Create Comparison.tsx wrapper**

Create `src/components/Comparison/Comparison.tsx` as a `"use client"` component:
- Section wrapper: `<section className="bg-ink py-28">`
- Container: `<div className="mx-auto max-w-6xl px-6">`
- Section heading using `useTranslations("Comparison")` for title
- Heading: `<h2>` with standard heading style (`text-3xl text-text-primary font-normal md:text-4xl`, `letter-spacing: -0.03em`)
- Render `<ComparisonTable />` wrapped in `<div className="mt-12 hidden md:block">` (desktop only)
- Render `<ComparisonCards />` wrapped in `<div className="mt-12 md:hidden">` (mobile only, placeholder for now -- Task 2)
- For now, ComparisonCards can be a simple placeholder `<div>` to unblock build verification
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- ComparisonTable renders 5 products x 6 dimensions with semantic HTML table
- Dictus column highlighted with accent border and glow background
- Sticky header positioned at top-16 (below Nav)
- Check/cross icons have sr-only labels
- i18n data complete in both en.json and fr.json
- Build passes with no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile card stack + stagger animation + page integration</name>
  <files>src/components/Comparison/ComparisonCards.tsx, src/components/Comparison/Comparison.tsx, src/app/[locale]/page.tsx</files>
  <action>
**Step 1: Create ComparisonCards.tsx (mobile)**

Create `src/components/Comparison/ComparisonCards.tsx` as a `"use client"` component:
- Use `useTranslations("Comparison")` for all text
- Use `useState<string | null>(null)` for expanded card tracking (null = only Dictus expanded)
- Render 5 product cards stacked vertically, Dictus card first
- Each card uses Tier 2 glass styling: `rounded-2xl border border-border bg-[var(--glass-t2-bg)] backdrop-blur-[12px] backdrop-saturate-[1.2]`
- Dictus card:
  - Always expanded (no toggle)
  - Accent highlight: `border-accent/30` border instead of `border-border`, plus `bg-[rgba(61,126,255,0.12)]` overlay
  - Shows all 6 feature rows with check/cross icons and labels
- Other 4 competitor cards:
  - Collapsed by default: show product name + tap indicator (chevron)
  - On tap: expand to show all 6 feature rows
  - Use `m.div` with `AnimatePresence` from `motion/react` for smooth height animation:
    - `initial={{ height: 0, opacity: 0 }}`
    - `animate={{ height: "auto", opacity: 1 }}`
    - `exit={{ height: 0, opacity: 0 }}`
    - `transition={{ duration: 0.3, ease: "easeInOut" }}`
  - `overflow-hidden` on the animated container
- Each feature row within a card: label on left, icon/text on right, using flexbox
- Reuse the same CheckIcon/CrossIcon inline SVGs from ComparisonTable (extract to shared constants or duplicate -- executor's discretion)
- Cards stagger on scroll entry using `m.div` wrapper with staggerChildren (~80ms)

**Step 2: Update Comparison.tsx**

Replace the placeholder ComparisonCards `<div>` with the actual `<ComparisonCards />` import.

**Step 3: Integrate into page.tsx**

In `src/app/[locale]/page.tsx`:
- Add `import Comparison from "@/components/Comparison/Comparison";`
- Insert `<ScrollReveal><Comparison /></ScrollReveal>` between `<ScrollReveal><Features /></ScrollReveal>` and `<ScrollReveal><HowItWorks /></ScrollReveal>`

Ensure no `overflow: hidden` is added on any ancestor wrapper that could break sticky header on desktop.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Mobile cards render at <768px with Dictus expanded and others collapsed
- Tapping a competitor card expands it with smooth animation
- Cards use Tier 2 glass styling consistent with site
- Comparison section visible on page between Features and HowItWorks
- Stagger animation triggers on scroll entry
- Build passes with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Desktop (>=768px): semantic HTML table visible with 5 columns, 6 rows, sticky header, Dictus column highlighted
- Mobile (<768px): card stack with Dictus expanded, others collapsed with tap-to-expand
- Both /fr and /en routes show translated comparison data
- Check/cross icons have sr-only text for accessibility
- No overflow:hidden on ancestors breaking sticky positioning
</verification>

<success_criteria>
- Comparison section renders between Features and HowItWorks
- Desktop table has sticky header, accent-highlighted Dictus column, stagger animation
- Mobile cards have expand/collapse with Dictus always expanded
- All text bilingual (FR/EN) via next-intl
- Build green, no hydration warnings
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-cta/07-01-SUMMARY.md`
</output>
