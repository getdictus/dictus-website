---
phase: 11-donation-page
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/messages/fr.json
  - src/messages/en.json
  - src/components/Nav/Nav.tsx
  - src/components/Footer/Footer.tsx
  - src/components/Donate/DonateCards.tsx
  - src/app/[locale]/donate/page.tsx
autonomous: true
gap_closure: true
requirements: [DON-01, DON-05]

must_haves:
  truths:
    - "No user-facing string or i18n key on the site uses 'Contribuer' or 'Contribute' — replaced by 'Soutenir' / 'Support' everywhere"
    - "Nav pill button label key is Nav.support_label in FR ('Soutenir') and EN ('Support')"
    - "Footer link label key is Footer.support_link in FR ('Soutenir') and EN ('Support')"
    - "Donate page FR custom button reads 'Soutenir' (EN: 'Support')"
    - "Donate page FR heading remains 'Soutenir Dictus' (unchanged) and meta_title reads 'Soutenir Dictus — Contribution'"
    - "@iconify/react is a direct dependency in package.json"
    - "npm run build succeeds and generates /fr/donate and /en/donate"
  artifacts:
    - path: "package.json"
      provides: "@iconify/react dependency"
      contains: "@iconify/react"
    - path: "src/messages/fr.json"
      provides: "Renamed i18n keys and values (Contribuer -> Soutenir)"
      contains: "support_label"
    - path: "src/messages/en.json"
      provides: "Renamed i18n keys and values (Contribute -> Support)"
      contains: "support_label"
    - path: "src/components/Nav/Nav.tsx"
      provides: "Uses t('support_label') for the pill button"
      contains: "support_label"
    - path: "src/components/Footer/Footer.tsx"
      provides: "Uses t('support_link') for the footer link"
      contains: "support_link"
    - path: "src/components/Donate/DonateCards.tsx"
      provides: "Uses renamed Donate keys (no references to custom_submit='Contribuer')"
      contains: "custom_submit"
    - path: "src/app/[locale]/donate/page.tsx"
      provides: "Unchanged structure but pulls renamed keys"
  key_links:
    - from: "src/components/Nav/Nav.tsx"
      to: "src/messages/fr.json (Nav.support_label)"
      via: "t('support_label')"
      pattern: 'support_label'
    - from: "src/components/Footer/Footer.tsx"
      to: "src/messages/fr.json (Footer.support_link)"
      via: "t('support_link')"
      pattern: 'support_link'
    - from: "src/components/Donate/DonateCards.tsx"
      to: "@iconify/react"
      via: "future usage in plan 11-05; dependency available at install time"
      pattern: '@iconify/react'
---

<objective>
Atomic terminology rename (Contribuer/Contribute -> Soutenir/Support) across all i18n keys, values, and call sites; install @iconify/react dependency for upcoming card icon swap.

Purpose: Resolve UAT gap #1 root cause (a) — "Contribuer" feels like open-source dev contribution, not financial support. Also lay the foundation for the card visual rework in plan 11-05 by having Iconify available.
Output: Every user-facing FR/EN string and i18n key on the site uses "Soutenir"/"Support"; `@iconify/react` installed; build passes.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-donation-page/11-CONTEXT.md
@.planning/phases/11-donation-page/11-UAT.md
@CLAUDE.md

<interfaces>
<!-- Current i18n keys that must be renamed -->
Current FR (src/messages/fr.json):
  Nav.contribute_label = "Contribuer"
  Footer.contribute_link = "Contribuer"
  Donate.meta_title = "Soutenir Dictus - Contribution"     (unchanged — already "Soutenir")
  Donate.heading = "Soutenir Dictus"                       (unchanged — already "Soutenir")
  Donate.custom_submit = "Contribuer"                       -> becomes "Soutenir"
  Donate.motivation: contains "Votre contribution aide..." -> KEEP (the abstract noun "contribution" is allowed, only the verb "Contribuer" is problematic)

Current EN (src/messages/en.json):
  Nav.contribute_label = "Contribute"
  Footer.contribute_link = "Contribute"
  Donate.meta_title = "Support Dictus - Contribution"      (unchanged — already "Support")
  Donate.heading = "Support Dictus"                        (unchanged — already "Support")
  Donate.custom_submit = "Contribute"                      -> becomes "Support"
  Donate.motivation: contains "Your contribution..."       -> KEEP (same reasoning as FR)

Current call sites:
  src/components/Nav/Nav.tsx (line 37): t("contribute_label")
  src/components/Footer/Footer.tsx (line 87): t("contribute_link")
  src/components/Donate/DonateCards.tsx (lines 122, 148, 191, 217): t("custom_submit") — key name unchanged, only value changes
  src/app/[locale]/donate/page.tsx: no contribute strings (uses meta_title, heading, motivation, thank_you only)
</interfaces>

<design_decisions_from_uat>
Verbatim from 11-UAT.md "Design Decisions (agreed with user 2026-04-14)":

- Terminology: rename "Contribuer/Contribute" -> "Soutenir/Support" everywhere (i18n keys, Nav pill, Footer link, donate page copy, page metadata, HTML title, OG tags).
  - i18n keys: `Nav.contribute_label` -> `Nav.support_label`, `Footer.contribute_link` -> `Footer.support_link`, donate page namespace updated accordingly.
- Icon library: install `@iconify/react` (pick latest stable, tree-shaken on-demand loading). No other icon packs — Iconify sources everything.
</design_decisions_from_uat>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @iconify/react dependency</name>
  <files>package.json</files>
  <read_first>
    - package.json (current dependencies)
    - .planning/phases/11-donation-page/11-UAT.md (Design Decisions section — icon library directive)
  </read_first>
  <action>
Install the latest stable `@iconify/react` package and add it as a direct runtime dependency (not devDependency).

Command:
```bash
cd /Users/pierreviviere/dev/dictus-website && npm install @iconify/react
```

After install, verify package.json "dependencies" contains `@iconify/react` with a version string, and a `package-lock.json` change is present (if the project uses npm lockfile).

Do NOT install any other icon package — Iconify sources all icons via on-demand loading at runtime. No separate `@iconify-icons/*` packs needed for `<Icon icon="solar:card-bold-duotone" />` usage pattern.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && node -e "const p=require('./package.json'); if(!p.dependencies['@iconify/react']) process.exit(1); console.log('ok', p.dependencies['@iconify/react'])"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` "dependencies" object contains a key `"@iconify/react"` with a version string (e.g. `"^5.x"`)
    - `node_modules/@iconify/react/package.json` exists after install
    - No other icon packages added (no `@iconify-icons/*`, no `lucide-react`, no `react-icons`)
  </acceptance_criteria>
  <done>@iconify/react installed as a runtime dependency; package.json updated; project installs cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename Contribuer/Contribute -> Soutenir/Support across i18n and call sites</name>
  <files>src/messages/fr.json, src/messages/en.json, src/components/Nav/Nav.tsx, src/components/Footer/Footer.tsx, src/components/Donate/DonateCards.tsx</files>
  <read_first>
    - src/messages/fr.json (find all Contribuer keys and values)
    - src/messages/en.json (find all Contribute keys and values)
    - src/components/Nav/Nav.tsx (call site for t("contribute_label"))
    - src/components/Footer/Footer.tsx (call site for t("contribute_link"))
    - src/components/Donate/DonateCards.tsx (uses t("custom_submit") in chip aria-labels and custom buttons)
    - src/app/[locale]/donate/page.tsx (verify it only uses meta_title/heading/motivation/thank_you — no rename needed)
    - .planning/phases/11-donation-page/11-UAT.md (Design Decisions — verbatim terminology directive)
    - CLAUDE.md (brand tone — confirm "Soutenir" fits premium tone; it does)
  </read_first>
  <action>
Perform the rename atomically in one commit. Every change below MUST be applied.

**1. `src/messages/fr.json`** — apply these exact edits:

- Inside the `"Nav"` object, change the key:
  - FROM: `"contribute_label": "Contribuer"`
  - TO:   `"support_label": "Soutenir"`

- Inside the `"Footer"` object, change the key:
  - FROM: `"contribute_link": "Contribuer"`
  - TO:   `"support_link": "Soutenir"`

- Inside the `"Donate"` object, change the value (key name stays `custom_submit` — it's a semantic UX slot, not terminology):
  - FROM: `"custom_submit": "Contribuer"`
  - TO:   `"custom_submit": "Soutenir"`

Keep every other key/value identical. `Donate.motivation` already reads "Votre contribution aide..." — KEEP it; the abstract noun "contribution" is fine per the Design Decisions (only the verb "Contribuer" is being replaced). `Donate.heading` stays "Soutenir Dictus" (already correct). `Donate.meta_title` stays "Soutenir Dictus - Contribution" (already correct).

**2. `src/messages/en.json`** — apply these exact edits:

- Inside the `"Nav"` object:
  - FROM: `"contribute_label": "Contribute"`
  - TO:   `"support_label": "Support"`

- Inside the `"Footer"` object:
  - FROM: `"contribute_link": "Contribute"`
  - TO:   `"support_link": "Support"`

- Inside the `"Donate"` object:
  - FROM: `"custom_submit": "Contribute"`
  - TO:   `"custom_submit": "Support"`

Keep every other key/value identical. `Donate.motivation` keeps "Your contribution helps..." (noun form allowed). `Donate.heading` stays "Support Dictus". `Donate.meta_title` stays "Support Dictus - Contribution".

**3. `src/components/Nav/Nav.tsx`** (currently line 37 reads `{t("contribute_label")}`):

- Replace the single occurrence of `t("contribute_label")` with `t("support_label")`.
- Leave every other line unchanged (import, styling, href="/donate").

**4. `src/components/Footer/Footer.tsx`** (currently line 87 reads `{t("contribute_link")}`):

- Replace the single occurrence of `t("contribute_link")` with `t("support_link")`.
- Leave the separator dot and surrounding `<Link href="/donate">` unchanged.

**5. `src/components/Donate/DonateCards.tsx`**:

- The key `custom_submit` is NOT being renamed (it describes the button's purpose, not the terminology). The value changes (handled in step 1+2 above).
- However, VERIFY the component file does not contain the raw string `"Contribuer"` or `"Contribute"` anywhere (e.g., in comments, aria-labels, constants). If any hard-coded string exists, remove/replace it to use `t("custom_submit")` instead.
- Search the file for: `Contribuer`, `Contribute`. Expected result: zero matches. If a match is found, replace with a `t()` call.

**6. `src/app/[locale]/donate/page.tsx`** — no changes required; verify no `contribute` or `Contribuer` string references exist. If any are found (unexpected), replace with the appropriate Donate namespace key (`heading` / `meta_title`).

**Post-edit verification** (run these greps after edits):

```bash
cd /Users/pierreviviere/dev/dictus-website
grep -rn "contribute_label\|contribute_link" src/ || echo "no matches — OK"
grep -rn "Contribuer" src/ || echo "no matches — OK"
grep -rn "\"Contribute\"" src/messages/ || echo "no matches — OK"
grep -rn "support_label" src/
grep -rn "support_link" src/
```

The first three must return "no matches — OK". The last two must return at least one hit each (in messages + one in Nav.tsx / Footer.tsx respectively).

**Build verification:**

```bash
npm run build
```

Must exit 0. SSG must generate `/fr/donate` and `/en/donate` without i18n key errors (next-intl throws at build time if a component calls `t("missing_key")`).
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "contribute_label" src/` returns NO matches
    - `grep -rn "contribute_link" src/` returns NO matches
    - `grep -rn "Contribuer" src/` returns NO matches
    - `grep -n "\"Contribute\"" src/messages/en.json` returns NO matches (the value "Contribute" must be gone from en.json; the noun "contribution" inside motivation paragraph is allowed)
    - `grep -n "support_label" src/messages/fr.json` returns a match with value `"Soutenir"`
    - `grep -n "support_label" src/messages/en.json` returns a match with value `"Support"`
    - `grep -n "support_link" src/messages/fr.json` returns a match with value `"Soutenir"`
    - `grep -n "support_link" src/messages/en.json` returns a match with value `"Support"`
    - `grep -n "custom_submit" src/messages/fr.json` returns a match with value `"Soutenir"`
    - `grep -n "custom_submit" src/messages/en.json` returns a match with value `"Support"`
    - `grep -n "t(\"support_label\")" src/components/Nav/Nav.tsx` returns exactly 1 match
    - `grep -n "t(\"support_link\")" src/components/Footer/Footer.tsx` returns exactly 1 match
    - Donate.heading still reads "Soutenir Dictus" in fr.json and "Support Dictus" in en.json (unchanged)
    - Donate.meta_title still reads "Soutenir Dictus - Contribution" in fr.json and "Support Dictus - Contribution" in en.json (unchanged)
    - `npm run build` exits 0
    - Build output shows `/fr/donate` and `/en/donate` are statically generated
  </acceptance_criteria>
  <done>All i18n keys and user-facing string values that said "Contribuer/Contribute" now say "Soutenir/Support". Every call site updated. Build passes. No leftover references.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 and generates `/fr/donate` + `/en/donate`.
2. `grep -rn "contribute_label\|contribute_link\|Contribuer" src/` returns nothing (excluding node_modules).
3. `grep -n "\"Contribute\"" src/messages/en.json` returns nothing (the raw value string is gone).
4. `grep -n "@iconify/react" package.json` returns a match.
5. Manual sanity: fr.json Donate.motivation still contains the noun "contribution" (allowed), but no value equals "Contribuer".
</verification>

<success_criteria>
- Zero user-facing uses of "Contribuer/Contribute" remain in i18n files or component call sites.
- Nav pill button and Footer link pull from `support_label` / `support_link` keys.
- Donate page custom button value reads "Soutenir" (FR) / "Support" (EN) via the unchanged `custom_submit` key.
- `@iconify/react` installed and ready for plan 11-05.
- Build is green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-donation-page/11-04-SUMMARY.md`
</output>
