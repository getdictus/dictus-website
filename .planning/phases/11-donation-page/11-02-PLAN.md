---
phase: 11-donation-page
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/Nav/Nav.tsx
  - src/components/Footer/Footer.tsx
  - src/messages/fr.json
  - src/messages/en.json
autonomous: true
requirements: [DON-01]

must_haves:
  truths:
    - "Nav bar displays an accent blue pill button labeled Contribuer (FR) / Contribute (EN) linking to /donate"
    - "Footer displays a Contribuer / Contribute text link after the Support link"
    - "Both entry points navigate to /[locale]/donate"
    - "Nav pill button is visible and tappable on 320px+ screens"
  artifacts:
    - path: "src/components/Nav/Nav.tsx"
      provides: "Accent pill button linking to /donate"
      contains: "contribute_label"
    - path: "src/components/Footer/Footer.tsx"
      provides: "Contribute text link in footer links row"
      contains: "contribute_link"
  key_links:
    - from: "src/components/Nav/Nav.tsx"
      to: "/[locale]/donate"
      via: "Link from @/i18n/navigation with href=/donate"
      pattern: 'href="/donate"'
    - from: "src/components/Footer/Footer.tsx"
      to: "/[locale]/donate"
      via: "Link from @/i18n/navigation with href=/donate"
      pattern: 'href="/donate"'
---

<objective>
Add "Contribuer" / "Contribute" entry points in the Nav bar (accent pill button) and Footer (text link) linking to /donate.

Purpose: Make the donation page discoverable from every page on the site via two navigation entry points.
Output: Updated Nav.tsx with accent pill button and Footer.tsx with text link, both using i18n labels.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-donation-page/11-CONTEXT.md
@.planning/phases/11-donation-page/11-UI-SPEC.md
@.planning/phases/11-donation-page/11-01-SUMMARY.md

<interfaces>
<!-- Current Nav.tsx structure (from codebase) -->
From src/components/Nav/Nav.tsx:
```typescript
"use client";
import { useState, useEffect } from "react";
import Logo from "./Logo";
import LanguageToggle from "./LanguageToggle";

// JSX: <div className="flex items-center gap-1">
//        <LanguageToggle />
//      </div>
```
Nav is a client component. Add useTranslations("Nav") and Link from @/i18n/navigation.

<!-- Current Footer.tsx structure (from codebase) -->
From src/components/Footer/Footer.tsx:
```typescript
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
// Already uses useTranslations("Footer") and Link

// Links row at line 68-81:
// <Link href="/privacy">...</Link>
// <span aria-hidden>.</span>
// <Link href="/support">...</Link>
```
Footer already imports useTranslations and Link. Just add the contribute link after support.

<!-- i18n keys needed (added by Plan 01 to Donate namespace, but Nav/Footer need their own keys) -->
Nav namespace needs: "contribute_label": "Contribuer" (FR) / "Contribute" (EN)
Footer namespace needs: "contribute_link": "Contribuer" (FR) / "Contribute" (EN)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Nav pill button and Footer link with i18n keys</name>
  <files>src/components/Nav/Nav.tsx, src/components/Footer/Footer.tsx, src/messages/fr.json, src/messages/en.json</files>
  <read_first>
    - src/components/Nav/Nav.tsx (current structure — add pill button before LanguageToggle)
    - src/components/Footer/Footer.tsx (current structure — add link after Support link)
    - src/messages/fr.json (add Nav.contribute_label and Footer.contribute_link keys)
    - src/messages/en.json (add Nav.contribute_label and Footer.contribute_link keys)
    - .planning/phases/11-donation-page/11-UI-SPEC.md (Nav pill button and Footer link specs)
  </read_first>
  <action>
1. **Update `src/messages/fr.json`** — add to existing "Nav" namespace:
```json
"contribute_label": "Contribuer"
```
Add to existing "Footer" namespace:
```json
"contribute_link": "Contribuer"
```

2. **Update `src/messages/en.json`** — add to existing "Nav" namespace:
```json
"contribute_label": "Contribute"
```
Add to existing "Footer" namespace:
```json
"contribute_link": "Contribute"
```

3. **Update `src/components/Nav/Nav.tsx`:**
- Add imports: `import { useTranslations } from "next-intl";` and `import { Link } from "@/i18n/navigation";`
- Inside Nav component, add: `const t = useTranslations("Nav");`
- In the `<div className="flex items-center gap-1">`, add BEFORE `<LanguageToggle />`:
```tsx
<Link
  href="/donate"
  className="min-h-[44px] flex items-center rounded-full bg-accent px-4 py-2 text-sm font-normal text-white transition-colors hover:bg-accent-hi focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-ink-deep"
>
  {t("contribute_label")}
</Link>
```
- Update gap from `gap-1` to `gap-2` to give breathing room between pill and language toggle

4. **Update `src/components/Footer/Footer.tsx`:**
- After the Support link and its preceding dot separator (line ~81), add:
```tsx
<span className="text-white-40" aria-hidden="true">&middot;</span>
<Link
  href="/donate"
  className="text-white-40 transition-colors hover:text-white-70"
>
  {t("contribute_link")}
</Link>
```
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Nav/Nav.tsx contains `import { useTranslations } from "next-intl"`
    - src/components/Nav/Nav.tsx contains `import { Link } from "@/i18n/navigation"`
    - src/components/Nav/Nav.tsx contains `href="/donate"`
    - src/components/Nav/Nav.tsx contains `rounded-full bg-accent`
    - src/components/Nav/Nav.tsx contains `t("contribute_label")`
    - src/components/Nav/Nav.tsx contains `min-h-[44px]`
    - src/components/Footer/Footer.tsx contains `href="/donate"`
    - src/components/Footer/Footer.tsx contains `t("contribute_link")`
    - src/messages/fr.json Nav namespace contains `"contribute_label": "Contribuer"`
    - src/messages/fr.json Footer namespace contains `"contribute_link": "Contribuer"`
    - src/messages/en.json Nav namespace contains `"contribute_label": "Contribute"`
    - src/messages/en.json Footer namespace contains `"contribute_link": "Contribute"`
    - `npx next build` succeeds (exit code 0)
  </acceptance_criteria>
  <done>Nav displays accent blue pill button "Contribuer"/"Contribute" before LanguageToggle linking to /donate. Footer displays "Contribuer"/"Contribute" text link after Support link. Both use locale-aware Link component. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx next build` passes
2. `grep 'href="/donate"' src/components/Nav/Nav.tsx` returns match
3. `grep 'href="/donate"' src/components/Footer/Footer.tsx` returns match
4. `grep 'contribute_label' src/messages/fr.json` returns match
5. `grep 'contribute_link' src/messages/fr.json` returns match
</verification>

<success_criteria>
- Nav pill button visible with accent blue background, linking to /donate
- Footer text link visible after Support link, linking to /donate
- Both use i18n labels from Nav and Footer namespaces
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-donation-page/11-02-SUMMARY.md`
</output>
