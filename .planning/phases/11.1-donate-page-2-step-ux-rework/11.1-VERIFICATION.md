---
phase: 11.1-donate-page-2-step-ux-rework
verified: 2026-04-16T06:10:00Z
status: passed
score: 8/8 must-haves verified
---

# Phase 11.1: Donate page 2-step UX rework — Verification Report

**Phase Goal:** Rework the /donate page into a 2-step flow (Step 1 = pick method, Step 2 = pick amount + confirm). Eliminate the duplicate-card redundancy, the native-feeling mismatch between chip-click redirect and unused Support CTA, and the off-brand navy CTA color. After rework, a user sees one hero method-picker; after choosing, they see a single amount card whose Support button is the SOLE redirect trigger.

**Verified:** 2026-04-16T06:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | Step 1 shows two large clickable method-picker cards (Credit card + Bitcoin) side-by-side at sm+ / stacked at xs; no chips, input, or amount CTA visible | VERIFIED | `src/components/Donate/DonateCards.tsx:109-164` — `if (step === "idle")` branch returns only the grid (`grid-cols-1 sm:grid-cols-2`) with two `<button>` method cards, no chip grid / input / CTA in this branch |
| 2 | Clicking a method card transitions to a single centered amount card with method header tile, 4-col chip grid (5/10/25/50 €), custom digits-only input + stepper, and full-width Soutenir/Support CTA | VERIFIED | `DonateCards.tsx:111-161` onClick handlers call `setStep("fiat" \| "bitcoin")`; Step 2 branch `182-383` renders `amountCardClass` (`max-w-[480px]`, `mx-auto`), tile header, `grid-cols-2 sm:grid-cols-4` chip grid over `AMOUNTS` (5/10/25/50), stepper input block `250-312`, CTA `317-381` |
| 3 | Chip click does NOT redirect (state only); custom input does NOT redirect; CTA is the SOLE redirect trigger | VERIFIED | Chips: `onClick={() => selectChip(amount)}` only calls `setSelectedChip/setCustomAmount/setError` (`DonateCards.tsx:53-57, 240`). Input `onChange={(e) => onCustomChange(e.target.value)}` only updates state (`59-63, 263`). Redirect artifacts (`<a href>` / `<form action>`) appear exclusively on the CTA (`330-336, 351-380`) |
| 4 | CTA disabled (muted surface, cursor-not-allowed, aria-disabled=true) when no amount selected; clicking does nothing | VERIFIED | `ctaDisabled = activeAmount === null` (`44`); both fiat and bitcoin branches render `<button disabled aria-disabled="true" style={ctaDisabledStyle} className={ctaDisabledClass}>` when disabled (`319-328, 340-349`); `ctaDisabledClass` includes `cursor-not-allowed` (`98`); `ctaDisabledStyle` sets muted surface rgba(255,255,255,0.06) + muted text rgba(255,255,255,0.35) (`99-102`) |
| 5 | Enabled CTA uses Accent Blue gradient `linear-gradient(135deg, #3D7EFF, #2563EB)` with hover `box-shadow 0 8px 24px rgba(61,126,255,0.35)`; old navy `#1A4E8A → #0F3460` gone from DonateCards.tsx | VERIFIED | `ctaEnabledStyle.backgroundImage = "linear-gradient(135deg, #3D7EFF, #2563EB)"` (`91`); `ctaEnabledClass` contains `hover:shadow-[0_8px_24px_rgba(61,126,255,0.35)]` (`94`). Grep for `#1A4E8A\|#0F3460` in DonateCards.tsx returns 0 matches |
| 6 | Fiat CTA is `<a href={stripeLinks[...]}>` with preset/custom branch; Bitcoin CTA is `<button type="submit">` in `<form action=${btcpayConfig.serverUrl}/api/v1/invoices method=POST>` with storeId/price/currency/checkoutDesc hidden inputs | VERIFIED | Fiat href resolver `171-180` branches on `activeAmount` over `[5,10,25,50]`, falls back to `stripeLinks.custom`; rendered as `<a href={fiatHref}>` (`330-336`). Bitcoin form `351-380` uses `action={\`${btcpayConfig.serverUrl}/api/v1/invoices\`}`, `method="POST"`, `target="_blank"`, and four hidden inputs (storeId, price=String(activeAmount), currency="EUR", checkoutDesc="Contribution Dictus") wrapping `<button type="submit">` |
| 7 | Back link at top-left of Step 2 card resets state to 'idle' and clears selectedAmount, returning to Step 1 | VERIFIED | `resetToIdle()` (`46-51`) sets step="idle", selectedChip=null, customAmount="", error=null. Back button `185-198` calls `onClick={resetToIdle}` with aria-label `t("change_method")` |
| 8 | "Why support?" inline-bullets section from Phase 11-05 renders below the card in BOTH step states, unchanged | VERIFIED | `src/app/[locale]/donate/page.tsx:45-75` — `<section aria-labelledby="why-support-title">` is rendered unconditionally after `<DonateCards />`, so it appears in every DonateCards render branch (Step 1 and Step 2). Only text tokens were swapped (`text-mist` → `text-white-70`), section markup unchanged |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/components/Donate/DonateCards.tsx` | 2-step stateful component with `useState<"idle" \| "fiat" \| "bitcoin">`, disabled-until-amount-selected CTA, Accent Blue gradient | VERIFIED | `Step` type defined line 27; `useState<Step>("idle")` line 31; file 384 lines, substantive implementation with no TODO/FIXME/PLACEHOLDER |
| `src/messages/fr.json` | New FR keys: method_fiat_title, method_fiat_subtitle, method_bitcoin_title, method_bitcoin_subtitle, method_bitcoin_lightning_tag, change_method, amount_prompt | VERIFIED | Lines 156-162: all 7 keys present with correct FR values (e.g. `"method_fiat_title": "Carte bancaire"`, `"amount_prompt": "Choisissez un montant (€)"`, `"method_bitcoin_lightning_tag": "⚡ Lightning supporté"`). JSON parses clean |
| `src/messages/en.json` | Same 7 keys in EN | VERIFIED | Lines 156-162: all 7 keys present with EN values (e.g. `"method_fiat_title": "Credit card"`, `"amount_prompt": "Pick an amount (€)"`, `"method_bitcoin_lightning_tag": "⚡ Lightning supported"`). JSON parses clean |
| `src/app/[locale]/donate/page.tsx` | Page shell keeps Why-support section across all step states; DonateCards still renders | VERIFIED | Line 4: `import DonateCards`; line 44: `<DonateCards />`; lines 45-75: Why-support `<section>` rendered unconditionally; structure otherwise unchanged |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `DonateCards.tsx` | `src/config/donate.ts` | `stripeLinks[amount]` / `stripeLinks.custom` / `btcpayConfig.serverUrl` / `btcpayConfig.storeId` | WIRED | Lines 6-12 import all 5 symbols; lines 173-180 use `stripeLinks[5..50]` + `stripeLinks.custom`; lines 352, 360 use `btcpayConfig.serverUrl` and `btcpayConfig.storeId` |
| `DonateCards.tsx` | `messages/{fr,en}.json` Donate namespace | `useTranslations('Donate') + t(...)` | WIRED | Line 30 `const t = useTranslations("Donate")`. 20 `t(...)` calls found including `t("method_fiat_title")`, `t("method_fiat_subtitle")`, `t("method_bitcoin_title")`, `t("method_bitcoin_subtitle")`, `t("method_bitcoin_lightning_tag")`, `t("change_method")`, `t("amount_prompt")`, `t("custom_submit")`, `t("custom_placeholder")` |
| Step 1 method card onClick | `setStep('fiat' \| 'bitcoin')` | React state transition, no URL change | WIRED | Lines 114, 137: both method buttons call `onClick={() => setStep("fiat")}` / `setStep("bitcoin")` — pure state, no router, no remount |
| Step 2 CTA click (enabled) | Stripe Payment Link redirect / BTCPay form POST | `<a href={stripeLinks[...]}>` for fiat; `<form action=btcpay>` `<button type="submit">` for bitcoin | WIRED | Fiat: `<a href={fiatHref}>` line 330-336. Bitcoin: `<form action={`${btcpayConfig.serverUrl}/api/v1/invoices`} method="POST" target="_blank">` wrapping `<button type="submit">` lines 351-380 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| DON-01 | 11.1-01-PLAN.md | Page /donate bilingue FR/EN accessible depuis le site, avec deux cartes (Fiat et Bitcoin) | SATISFIED (reworked) | Bilingual FR/EN routes intact; Fiat + Bitcoin methods remain selectable entry points (now as method-picker cards in Step 1). The original "cote a cote" wording is explicitly superseded by Phase 11.1's 2-step goal approved in ROADMAP.md |
| DON-02 | 11.1-01-PLAN.md | Carte Fiat: montants 5/10/25/50 EUR + montant libre, redirige vers Stripe Payment Link | SATISFIED | AMOUNTS `[5,10,25,50]` mapped to chips (`DonateCards.tsx:232-247`); digits-only custom input with stepper (`250-310`); fiat CTA `<a href={fiatHref}>` resolves to `stripeLinks[5..50]` or `stripeLinks.custom` (`171-180, 330-336`) |
| DON-03 | 11.1-01-PLAN.md | Carte Bitcoin ouvre une invoice BTCPay Server | SATISFIED | Bitcoin CTA renders `<form action="${btcpayConfig.serverUrl}/api/v1/invoices" method="POST" target="_blank">` with hidden inputs storeId/price/currency/checkoutDesc and `<button type="submit">` (`351-380`) |
| DON-04 | 11.1-01-PLAN.md | Design coherent avec design system Dictus (dark, glassmorphism, accent blue, hover glow) | SATISFIED | `bg-[var(--glass-t2-bg)] backdrop-blur-[12px] backdrop-saturate-[1.2]` on method + amount cards (`67, 71`); Accent Blue gradient on CTA + tiles (`75, 77, 91`); hover glow `shadow-[0_8px_24px_rgba(61,126,255,0.35)]` on CTA (`94`); accent-tinted border on amount card (`71`). Brand presence strengthened vs Phase 11 |

All 4 declared requirements satisfied. No orphaned requirements: ROADMAP.md maps DON-01..04 to this phase and all 4 are covered by plan 11.1-01.

### Anti-Patterns Found

Scanned `src/components/Donate/DonateCards.tsx`, `src/app/[locale]/donate/page.tsx`, `src/messages/fr.json`, `src/messages/en.json`:

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No TODO / FIXME / XXX / HACK / PLACEHOLDER in any modified file | — | Clean |

Removed-code sanity check (from PLAN acceptance criteria):
- `handleStripeCustom` / `handleBtcCustom` / `handleBtcPayClick` / `_chipSelectedModifier` / `customFiatAmount` / `customBtcAmount` / `fiatError` / `btcError` — 0 occurrences remaining. Old imperative handlers and duplicate state fully removed.
- Old navy CTA gradient `linear-gradient(135deg, #1A4E8A, #0F3460)` — 0 occurrences. Fully removed.
- `inputMode="numeric"` — 1 occurrence (single custom input, down from 2). Duplicate input collapsed as expected.

### TypeScript / JSON Sanity

- `npx tsc --noEmit` exits 0 — no TypeScript errors.
- `JSON.parse` of both `src/messages/fr.json` and `src/messages/en.json` succeeds — no malformed JSON, no trailing commas.
- All 3 commits declared in SUMMARY (`0cb16ee`, `2010031`, `f4c77b6`) exist in git history.

### Informational Notes (not gaps)

**Tile gradient deviation vs PLAN.md action (user-approved, SUMMARY-documented):**
PLAN.md Task 2 originally specified navy tile gradient `linear-gradient(135deg,#0D2040,#071020)` for `tileLargeClass` / `tileSmallClass`. During live human verification (commit `f4c77b6`) the user requested swapping tiles to the brand Accent Blue gradient `linear-gradient(135deg,#3D7EFF,#2563EB)` for brand coherence with the nav Support button. This deviation is:
- NOT a violation of any must_haves truth (Truth #5 only mandates removal of `#1A4E8A → #0F3460` and use of `#3D7EFF → #2563EB` on the CTA).
- Explicitly documented in SUMMARY.md "Deviations from Plan" (fix #3) with user approval.
- Brand-aligned per CLAUDE.md (Accent Blue #3D7EFF is the brand CTA color; navy tiles clashed with the rest of the site's Accent Blue identity).
- Does not affect Stripe/BTCPay wiring or disabled-CTA semantics.

**Amount-card border deviation (same commit, same rationale):** border was swapped from `border-border` to `border-[rgba(61,126,255,0.25)]` with `hover:border-accent`. Not mentioned in any must_haves truth — purely a brand-presence polish.

**Back-arrow rendering deviation (bug fix):** Unicode `←` replaced with Iconify `solar:arrow-left-linear` at 16x16 because DM Sans metrics misaligned the glyph above the baseline in `inline-flex items-center`. Documented in SUMMARY.md deviations #2.

**Text-token deviation (bug fix):** `text-mist` (static `#DBEAFE`) swapped for theme-aware `text-text-primary` / `text-white-70` on chips, Why-support bullets, and thank-you footer to fix a light-theme invisibility regression. Documented in SUMMARY.md deviations #1. Section markup on page.tsx otherwise unchanged (Truth #8 preserved).

### Human Verification Required

None outstanding. The user has already performed live browser verification (`localhost:3000/fr/donate` and `/en/donate`, both themes) and approved — confirmed in SUMMARY.md "Must-haves Verification" and the three follow-up polish commits (`f4c77b6`).

### Gaps Summary

No gaps. All 8 observable truths verified in the codebase. All 4 artifacts present, substantive, and properly wired. All 4 key links functional. All 4 declared requirements (DON-01..04) satisfied. No anti-patterns, no stubs, no orphaned state.

The phase goal — "one hero method-picker; after choosing, a single amount card whose Support button is the SOLE redirect trigger" — is fully realized.

---

*Verified: 2026-04-16T06:10:00Z*
*Verifier: Claude (gsd-verifier)*
