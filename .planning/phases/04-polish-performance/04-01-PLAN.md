---
phase: 04-polish-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/globals.css
  - src/messages/fr.json
  - src/messages/en.json
  - public/og-image.png
autonomous: true
requirements: [SEO-01, SEO-02, PERF-01]

must_haves:
  truths:
    - "Sharing site URL on social media shows correct OG title, description, and image per locale"
    - "Search engines see hreflang tags linking FR/EN alternates with x-default pointing to /fr"
    - "Visiting /sitemap.xml returns valid sitemap with both locales and hreflang alternates"
    - "Visiting /robots.txt returns valid robots file with sitemap reference"
    - "JSON-LD structured data (MobileApplication) is present in page HTML"
    - "Skip-to-content link appears on keyboard Tab press"
    - "Secondary text meets WCAG AA 4.5:1 contrast ratio on dark background"
    - "Canvas waveform has aria-hidden and role=presentation"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap with hreflang alternates"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots.txt with sitemap URL"
      exports: ["default"]
    - path: "public/og-image.png"
      provides: "Static 1200x630 OG image"
    - path: "src/app/[locale]/layout.tsx"
      provides: "generateMetadata with per-locale OG, hreflang, skip-to-content link"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/messages/fr.json"
      via: "getTranslations({locale, namespace: 'Metadata'})"
      pattern: "getTranslations.*Metadata"
    - from: "src/app/[locale]/layout.tsx"
      to: "public/og-image.png"
      via: "openGraph.images[].url"
      pattern: "og-image\\.png"
    - from: "src/app/sitemap.ts"
      to: "getdictus.com"
      via: "URL entries with hreflang alternates"
      pattern: "getdictus\\.com"
---

<objective>
Implement complete SEO metadata, accessibility fixes, and structured data for the dictus landing page.

Purpose: Ship production-ready SEO so the site is discoverable by search engines, renders correctly in social media shares, and passes accessibility audit.
Output: Per-locale generateMetadata with OG tags and hreflang, sitemap.xml, robots.txt, JSON-LD structured data, static OG image, skip-to-content link, contrast fix, ARIA improvements.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-performance/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ["fr", "en"],
  defaultLocale: "fr",
  localePrefix: "always",
});
```

From src/app/[locale]/layout.tsx (current -- will be replaced):
```typescript
export const metadata: Metadata = {
  title: "dictus",
  description: "Clavier iOS de dictation vocale 100% offline...",
};

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};
```

From src/messages/fr.json (Metadata namespace already exists):
```json
{
  "Metadata": {
    "title": "dictus -- Dictation vocale 100% offline pour iOS",
    "description": "Clavier iOS de dictation vocale qui fonctionne entierement sur votre appareil. Aucune donnee envoyee sur internet."
  }
}
```

From src/messages/en.json (Metadata namespace already exists):
```json
{
  "Metadata": {
    "title": "dictus -- 100% Offline Voice Dictation for iOS",
    "description": "iOS voice dictation keyboard that works entirely on your device. No data sent to the internet."
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO metadata, sitemap, robots, OG image, and i18n message updates</name>
  <files>src/app/[locale]/layout.tsx, src/app/sitemap.ts, src/app/robots.ts, src/messages/fr.json, src/messages/en.json, public/og-image.png</files>
  <action>
1. **Replace static metadata with generateMetadata in layout.tsx:**
   - Remove `export const metadata: Metadata` object entirely (cannot coexist with generateMetadata).
   - Add `import { getTranslations } from "next-intl/server"` (already partially imported -- add getTranslations).
   - Create `export async function generateMetadata({ params }: Props): Promise<Metadata>` that:
     - Awaits `params` to get `locale`
     - Uses `getTranslations({ locale, namespace: "Metadata" })` for localized strings
     - Sets `metadataBase: new URL("https://getdictus.com")` (CRITICAL for OG image resolution)
     - Returns `title: t("title")`, `description: t("description")`, `applicationName: "dictus"`
     - Returns `openGraph` with: title, description, `url: \`/\${locale}\``, siteName "dictus", images `[{ url: "/og-image.png", width: 1200, height: 630, alt: "dictus" }]`, locale (fr_FR or en_US), type "website"
     - Returns `twitter` with: card "summary_large_image", title, description, images
     - Returns `alternates` with: `canonical: \`/\${locale}\``, `languages: { fr: "/fr", en: "/en", "x-default": "/fr" }`
     - Returns `robots: { index: true, follow: true }`

2. **Add JSON-LD structured data in page.tsx** (not layout.tsx -- avoid duplicate rendering):
   - Import `getTranslations` from "next-intl/server"
   - After `setRequestLocale(locale)`, get `const t = await getTranslations("Metadata")`
   - Build jsonLd object: `@context: "https://schema.org"`, `@type: "MobileApplication"`, `name: "dictus"`, `description: t("jsonld_description")`, `operatingSystem: "iOS 18+"`, `applicationCategory: "UtilitiesApplication"`, `offers: { @type: "Offer", price: "0", priceCurrency: "EUR" }`, `author: { @type: "Organization", name: "PIVI Solutions" }`
   - Render `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }} />` at the end of the JSX fragment (after Footer)

3. **Create src/app/sitemap.ts** (at app root, NOT inside [locale]):
   - Export default function returning `MetadataRoute.Sitemap`
   - Two entries: /fr (priority 1) and /en (priority 0.9), both with `changeFrequency: "monthly"`, `lastModified: new Date()`
   - Each entry has `alternates.languages: { fr: "https://getdictus.com/fr", en: "https://getdictus.com/en" }`
   - Use full absolute URLs with https://getdictus.com domain per user decision

4. **Create src/app/robots.ts** (at app root):
   - Export default function returning `MetadataRoute.Robots`
   - Rules: `userAgent: "*"`, `allow: "/"`
   - `sitemap: "https://getdictus.com/sitemap.xml"`

5. **Update i18n message files** -- add `jsonld_description` key to Metadata namespace in both:
   - fr.json: `"jsonld_description": "Clavier iOS de dictation vocale 100% offline avec transcription en temps reel sur l'appareil."`
   - en.json: `"jsonld_description": "Fully offline iOS voice dictation keyboard with real-time on-device transcription."`

6. **Create static OG image** at public/og-image.png:
   - 1200x630 pixels, dark #0A1628 background
   - Centered dictus waveform icon (three vertical bars with asymmetric heights matching the brand squircle) and "dictus" wordmark below
   - Use brand colors: bars in gradient from #6BA3FF to #2563EB, wordmark in white
   - Create as SVG first, then convert to PNG using a one-off Node script with sharp or canvas. If neither is available, create the SVG and use `npx sharp-cli` or `npx svg2png-wasm` to convert. The image does NOT need to be pixel-perfect -- brand-forward and clean is sufficient.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build && echo "Build OK"</automated>
  </verify>
  <done>Build succeeds. generateMetadata replaces static metadata in layout.tsx. sitemap.ts and robots.ts exist at app root. JSON-LD script tag in page.tsx. OG image at public/og-image.png. Both message files have jsonld_description key.</done>
</task>

<task type="auto">
  <name>Task 2: Accessibility fixes -- contrast, skip-to-content, ARIA audit, canvas a11y</name>
  <files>src/app/globals.css, src/app/[locale]/layout.tsx, src/components/Hero/HeroDemo.tsx</files>
  <action>
1. **Bump secondary text contrast in globals.css:**
   - Change `--color-white-40: rgba(255, 255, 255, 0.40)` to `--color-white-40: rgba(255, 255, 255, 0.60)`
   - Keep the token NAME as `white-40` to avoid touching every component file (value changes, name stays)
   - Add a CSS comment: `/* WCAG AA: ~9.3:1 contrast on #0B0F1A */`

2. **Add skip-to-content link in layout.tsx:**
   - Import `useTranslations` is not needed here since layout is server component -- use `getTranslations` which is already imported for generateMetadata
   - Add a `const tNav = await getTranslations({ locale, namespace: "Nav" })` call (or reuse existing translations)
   - Before `<Nav />` inside `<body>`, add:
     ```
     <a href="#main-content" className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-lg focus:bg-accent focus:px-4 focus:py-2 focus:text-white focus:outline-none">
       {tNav("skip_to_content")}
     </a>
     ```
   - Add `id="main-content"` to the existing `<main>` element (currently has `className="pt-16"`)

3. **Add skip_to_content translation** to both message files:
   - fr.json Nav namespace: `"skip_to_content": "Aller au contenu principal"`
   - en.json Nav namespace: `"skip_to_content": "Skip to main content"`

4. **Canvas waveform accessibility** -- find the canvas element in HeroDemo.tsx (or whichever component renders the canvas waveform):
   - Add `aria-hidden="true"` and `role="presentation"` to the `<canvas>` element
   - This marks it as decorative per user decision

5. **ARIA audit of interactive elements** -- review and fix:
   - Nav language toggle button: ensure it has `aria-label` (e.g., "Switch language" / "Changer de langue")
   - CTA buttons/links: ensure they have descriptive text (likely already fine since they have visible text)
   - GitHub and Telegram links in Footer/OpenSource: ensure they have `aria-label` if they use icons without visible text
   - Any icon-only buttons: add appropriate `aria-label`
   - Grep for `<a `, `<button`, `<Link ` across components to find all interactive elements and verify labels
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build && echo "Build OK"</automated>
  </verify>
  <done>Secondary text token updated to 0.60 opacity. Skip-to-content link renders in layout before Nav, targets #main-content. Canvas has aria-hidden + role=presentation. All interactive elements have proper ARIA labels. Build succeeds.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds with no errors
2. `curl -s http://localhost:3000/fr | grep "og:title"` returns meta tag with French title
3. `curl -s http://localhost:3000/en | grep "og:title"` returns meta tag with English title
4. `curl -s http://localhost:3000/sitemap.xml | grep "hreflang"` returns hreflang entries
5. `curl -s http://localhost:3000/robots.txt` returns robots with sitemap URL
6. `curl -s http://localhost:3000/fr | grep "application/ld+json"` returns JSON-LD script
7. `curl -s http://localhost:3000/fr | grep "skip"` returns skip-to-content link
8. OG image file exists at public/og-image.png
</verification>

<success_criteria>
- Build succeeds with zero errors
- Both /fr and /en have locale-specific OG metadata (title, description, image, locale)
- hreflang alternates present in HTML head (fr, en, x-default)
- /sitemap.xml serves valid sitemap with both locale URLs and hreflang
- /robots.txt serves valid robots with sitemap reference
- JSON-LD MobileApplication schema present in page HTML
- Skip-to-content link visible on Tab key press
- Secondary text contrast meets WCAG AA (0.60 opacity on dark bg)
- Canvas waveform marked as decorative (aria-hidden, role=presentation)
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-performance/04-01-SUMMARY.md`
</output>
