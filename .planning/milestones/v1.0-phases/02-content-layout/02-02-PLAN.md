---
phase: 02-content-layout
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/Features/Features.tsx
  - src/components/HowItWorks/HowItWorks.tsx
  - src/components/DataFlow/DataFlow.tsx
  - src/components/OpenSource/OpenSource.tsx
  - src/components/Community/Community.tsx
  - src/components/Footer/Footer.tsx
  - src/app/[locale]/page.tsx
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04, FEAT-05, FOOT-01, DSGN-03, PRIV-02]

must_haves:
  truths:
    - "User can scroll through 4 glassmorphism feature cards showing privacy, smart mode, keyboard, and open source"
    - "User sees 3 how-it-works steps with arrow connectors on desktop, stacked on mobile"
    - "User sees a data flow diagram showing voice -> on-device model -> text with no cloud"
    - "User sees an open source section with a clickable GitHub link"
    - "User sees a community CTA banner with a Telegram button"
    - "User sees a footer with PIVI Solutions credit, GitHub/Telegram icons, and zero-data privacy statement"
    - "No more than 6 backdrop-filter blur elements are visible in any single viewport"
  artifacts:
    - path: "src/components/Features/Features.tsx"
      provides: "2x2 glassmorphism feature card grid"
      min_lines: 40
    - path: "src/components/HowItWorks/HowItWorks.tsx"
      provides: "3-step horizontal flow with arrow connectors"
      min_lines: 30
    - path: "src/components/DataFlow/DataFlow.tsx"
      provides: "Standalone glassmorphism data flow diagram card"
      min_lines: 25
    - path: "src/components/OpenSource/OpenSource.tsx"
      provides: "Open source section with GitHub link"
      min_lines: 15
    - path: "src/components/Community/Community.tsx"
      provides: "Full-width community CTA banner with Telegram button"
      min_lines: 15
    - path: "src/components/Footer/Footer.tsx"
      provides: "Footer with credits, icon links, privacy statement"
      min_lines: 25
    - path: "src/app/[locale]/page.tsx"
      provides: "Full page composition with all 7 sections"
      contains: "Footer"
  key_links:
    - from: "src/components/Features/Features.tsx"
      to: "src/messages/fr.json"
      via: "useTranslations('Features')"
      pattern: "useTranslations.*Features"
    - from: "src/components/Footer/Footer.tsx"
      to: "src/messages/fr.json"
      via: "useTranslations('Footer')"
      pattern: "useTranslations.*Footer"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/Footer/Footer.tsx"
      via: "import and render as last section"
      pattern: "import.*Footer"
---

<objective>
Build all remaining content sections (Features, How It Works, Data Flow, Open Source, Community, Footer) and compose the full page.

Purpose: After this plan, the complete landing page is readable top-to-bottom in both languages. A visitor can understand what Dictus is, how it works, and where to find it.

Output: 6 section components + updated page.tsx with full composition.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-layout/02-CONTEXT.md
@.planning/phases/02-content-layout/02-RESEARCH.md
@.planning/phases/02-content-layout/02-01-SUMMARY.md

<interfaces>
<!-- Translation keys available from Plan 01 -->
Namespaces in fr.json/en.json (created by Plan 01):
- Features: privacy_title, privacy_desc, smart_title, smart_desc, keyboard_title, keyboard_desc, opensource_title, opensource_desc
- HowItWorks: step1_title, step1_desc, step2_title, step2_desc, step3_title, step3_desc
- DataFlow: title, desc, no_cloud
- OpenSource: title, desc, cta
- Community: title, desc, cta
- Footer: copyright, privacy, github_label, telegram_label

<!-- Existing pattern from Hero (Plan 01) -->
From src/components/Hero/Hero.tsx:
```typescript
import { useTranslations } from "next-intl";
export default function Hero() {
  const t = useTranslations("Hero");
  // Server component, no "use client"
}
```

<!-- Page composition pattern -->
From src/app/[locale]/page.tsx (Plan 01 output):
```typescript
import { setRequestLocale } from "next-intl/server";
import Hero from "@/components/Hero/Hero";
// Plan 02 adds remaining imports + renders
```

<!-- Design tokens available -->
From globals.css @theme:
- Backgrounds: bg-ink-deep, bg-ink, bg-ink-2, bg-surface
- Borders: border-border (7%), border-border-hi (14%)
- Glows: shadow with --color-glow-soft
- Accent: text-accent, bg-accent/10, border-accent/30
- Text: text-white, text-white-70, text-white-40

<!-- Background color cycling (user decision) -->
Hero:        bg-ink-deep
Features:    bg-ink
HowItWorks:  bg-ink-2
DataFlow:    bg-ink-deep
OpenSource:  bg-ink
Community:   bg-ink-2
Footer:      bg-ink-deep
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Features and HowItWorks sections</name>
  <files>src/components/Features/Features.tsx, src/components/HowItWorks/HowItWorks.tsx</files>
  <action>
**Features component (src/components/Features/Features.tsx):**

Server component with `useTranslations("Features")`.

Section wrapper: `<section className="bg-ink py-28">` with inner `mx-auto max-w-6xl px-6`.

No section heading (user decision: no section headings, content flows as continuous narrative).

2x2 grid on desktop, 1 column on mobile: `grid grid-cols-1 gap-6 md:grid-cols-2`.

Each card uses full glassmorphism:
```
rounded-2xl border border-border bg-surface/50 p-6 backdrop-blur-md
hover:border-border-hi transition-colors
```

Each card contains:
1. Icon container: `flex h-12 w-12 items-center justify-center rounded-xl bg-navy/50` with an inline SVG icon inside
2. Title: `text-lg font-normal text-white mt-4`
3. Description: `text-sm leading-relaxed text-white-70 mt-2`

Icon choices (inline SVG, ~24x24, stroke-based, text-sky color):
- Privacy/Offline: Shield icon
- Smart Mode: Sparkle/brain icon
- Keyboard Integration: Keyboard icon
- Open Source: Code brackets icon (`</>`)

DSGN-03 constraint: These 4 cards use backdrop-blur. Combined with DataFlow card (1) = 5 blur elements. This is within the 6-element limit. Do NOT add backdrop-blur to any other section.

**HowItWorks component (src/components/HowItWorks/HowItWorks.tsx):**

Server component with `useTranslations("HowItWorks")`.

Section wrapper: `<section className="bg-ink-2 py-28">` with inner `mx-auto max-w-6xl px-6`.

No section heading.

Horizontal 3-step layout with arrow connectors on desktop, vertical stack on mobile:
```
flex flex-col items-center gap-8 md:flex-row md:justify-between
```

Each step:
- Circle container: `flex h-16 w-16 items-center justify-center rounded-full border border-border bg-surface/30` with inline SVG icon
- Step title below: `text-base font-normal text-white mt-3`
- Step description: `text-sm text-white-40 mt-1`

Icons for steps:
- Step 1 (tap mic): Microphone icon
- Step 2 (speak): Sound wave / voice icon
- Step 3 (text appears): Text/document icon

Arrow connectors between steps:
- Simple SVG chevron arrow (>) in text-white-40
- `hidden md:flex` -- only visible on desktop
- Vertically centered between steps

Do NOT use backdrop-blur on HowItWorks (would exceed DSGN-03 limit).
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && test -f src/components/Features/Features.tsx && test -f src/components/HowItWorks/HowItWorks.tsx && echo "Files exist"</automated>
  </verify>
  <done>Features renders 4 glassmorphism cards in a 2x2 grid with icons, titles, and descriptions. HowItWorks renders 3 steps with arrow connectors horizontally on desktop and stacked on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Build DataFlow, OpenSource, Community, and Footer sections</name>
  <files>src/components/DataFlow/DataFlow.tsx, src/components/OpenSource/OpenSource.tsx, src/components/Community/Community.tsx, src/components/Footer/Footer.tsx</files>
  <action>
**DataFlow component (src/components/DataFlow/DataFlow.tsx):**

Server component with `useTranslations("DataFlow")`.

Section wrapper: `<section className="bg-ink-deep py-28">` with inner `mx-auto max-w-6xl px-6`.

Standalone glassmorphism card (same treatment as feature cards): `rounded-2xl border border-border bg-surface/50 p-8 md:p-12 backdrop-blur-md`.

Inside the card:
- An inline SVG diagram showing the flow: Voice (mic icon) --> On-Device Model (device/chip icon) --> Text (document icon), with arrows connecting them. Use a horizontal flex layout. Each node is a circle with icon + label below.
- A prominent "no cloud" element: crossed-out cloud icon or text emphasis showing no data leaves the device.
- Title text from `t("title")`: `text-xl font-normal text-white`
- Description from `t("desc")`: `text-white-70 mt-3`
- No-cloud emphasis from `t("no_cloud")`: `text-sm text-accent font-light mt-4`

This is the 5th backdrop-blur element (4 feature cards + this). Still within DSGN-03 limit of 6.

**OpenSource component (src/components/OpenSource/OpenSource.tsx):**

Server component with `useTranslations("OpenSource")`.

Section wrapper: `<section className="bg-ink py-28">` with inner `mx-auto max-w-6xl px-6 text-center`.

Simple centered layout:
- Title: `text-2xl font-normal text-white`
- Description: `text-white-70 mt-4 max-w-2xl mx-auto`
- GitHub link: `<a href="https://github.com/Pivii/dictus" target="_blank" rel="noopener noreferrer">` styled as a subtle button/link with `inline-flex items-center gap-2 mt-6 text-accent hover:text-accent-hi transition-colors`. Include a small GitHub SVG icon. Ensure 44px minimum touch target (min-h-11 min-w-11 or adequate padding).

No live GitHub stats, no API calls (user decision: static link only).

**Community component (src/components/Community/Community.tsx):**

Server component with `useTranslations("Community")`.

Section wrapper: `<section className="bg-ink-2 py-28">` -- full-width banner feel.

Inner: `mx-auto max-w-6xl px-6 text-center`.

- Headline: `text-3xl md:text-4xl font-extralight text-white` (bigger for CTA impact)
- Description: `text-white-70 mt-4`
- Telegram button: `<a href="#" target="_blank" rel="noopener noreferrer">` styled as a prominent button: `inline-flex items-center gap-2 mt-8 rounded-full bg-accent px-8 py-3 text-white font-normal hover:bg-accent-hi transition-colors`. Include Telegram SVG icon. Min touch target 44px (the padding already exceeds this).

Placeholder href="#" for Telegram link (per user decision -- swap real URL later).

**Footer component (src/components/Footer/Footer.tsx):**

Server component with `useTranslations("Footer")`.

`<footer className="border-t border-border bg-ink-deep py-8">` with inner `mx-auto max-w-6xl px-6`.

Layout:
- Top row: flex justify-between items-center
  - Left: `<span className="text-sm text-white-40">` with copyright symbol + t("copyright")
  - Right: flex gap-4 with GitHub and Telegram icon links
    - Each link: `<a>` with `flex h-11 w-11 items-center justify-center text-white-40 hover:text-white-70 transition-colors` (44px touch target per PERF-02)
    - Use aria-label from translations for accessibility
    - GitHub: href="https://github.com/Pivii/dictus", target="_blank", rel="noopener noreferrer"
    - Telegram: href="#" (placeholder)
- Bottom row: `<p className="mt-4 text-xs text-white-40">` with t("privacy") -- this is the PRIV-02 zero-data statement

Inline SVG icons for GitHub (octocat outline) and Telegram (paper plane), ~20x20px.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && test -f src/components/DataFlow/DataFlow.tsx && test -f src/components/OpenSource/OpenSource.tsx && test -f src/components/Community/Community.tsx && test -f src/components/Footer/Footer.tsx && echo "All 4 files exist"</automated>
  </verify>
  <done>DataFlow renders a glassmorphism card with voice-to-text flow diagram. OpenSource has a static GitHub link. Community has a full-width CTA banner with Telegram button. Footer has credits, icon links with 44px touch targets, and PRIV-02 privacy statement.</done>
</task>

<task type="auto">
  <name>Task 3: Compose full page with all sections</name>
  <files>src/app/[locale]/page.tsx</files>
  <action>
Update page.tsx to import and render ALL 7 sections in the correct order (user decision):

1. Hero (already imported from Plan 01)
2. Features
3. HowItWorks
4. DataFlow
5. OpenSource
6. Community
7. Footer

Keep the existing async pattern with setRequestLocale. Render sections as a simple fragment stack (no wrapper div needed -- each section handles its own background color).

Remove any placeholder comments from Plan 01.

Verify the build passes with all sections composed. This is the integration point where everything comes together.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Full landing page renders all 7 sections in order: Hero > Features > HowItWorks > DataFlow > OpenSource > Community > Footer. Build passes. Both /fr and /en routes work with all bilingual content.</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- All 6 new component files exist in their respective directories
- page.tsx imports and renders all 7 sections
- Background colors cycle correctly: ink-deep > ink > ink-2 > ink-deep > ink > ink-2 > ink-deep
- Feature cards have visible glassmorphism (backdrop-blur-md)
- No more than 5 backdrop-blur elements in any viewport (4 feature cards + 1 data flow card)
- Footer shows PRIV-02 privacy statement
- GitHub and Telegram links in footer have 44px touch targets
- All content is bilingual (FR/EN switching works)
</verification>

<success_criteria>
- A visitor can scroll the complete page from hero to footer and read about Dictus
- All 4 feature cards render with glassmorphism styling
- How it works shows 3 connected steps
- Data flow diagram illustrates on-device processing
- Open source section links to GitHub
- Community CTA has a Telegram button
- Footer displays credits, links, and privacy statement
- FR/EN toggle switches all content
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-layout/02-02-SUMMARY.md`
</output>
