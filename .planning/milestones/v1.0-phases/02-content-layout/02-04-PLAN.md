---
phase: 02-content-layout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Nav/Nav.tsx
autonomous: true
gap_closure: true
requirements:
  - NAV-03

must_haves:
  truths:
    - "Nav background transitions from solid to glassmorphism blur when user scrolls past threshold"
    - "Nav returns to solid background when user scrolls back to top"
    - "Glassmorphism blur on nav does not exceed DSGN-03 limit of 6 blur elements per viewport"
  artifacts:
    - path: "src/components/Nav/Nav.tsx"
      provides: "Client component with scroll-aware glassmorphism nav"
      contains: "use client"
  key_links:
    - from: "src/components/Nav/Nav.tsx"
      to: "window scroll event"
      via: "useEffect + useState"
      pattern: "addEventListener.*scroll"
---

<objective>
Close the NAV-03 gap: add glassmorphism blur effect to the nav header when the user scrolls down the page.

Purpose: The Phase 2 success criterion requires "nav shows glassmorphism blur on scroll" but Nav.tsx currently uses a static `bg-ink-deep` background with no scroll-dependent behavior.
Output: Updated Nav.tsx as a client component with scroll-aware backdrop-blur transition.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/Nav/Nav.tsx
@src/components/Nav/Logo.tsx
@src/components/Nav/LanguageToggle.tsx

<interfaces>
<!-- Current Nav.tsx (13 lines, server component) -->
From src/components/Nav/Nav.tsx:
```typescript
import Logo from "./Logo";
import LanguageToggle from "./LanguageToggle";

export default function Nav() {
  return (
    <nav className="fixed top-0 left-0 right-0 z-50 border-b border-border bg-ink-deep">
      <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <Logo />
        <LanguageToggle />
      </div>
    </nav>
  );
}
```

<!-- Logo and LanguageToggle are already separate components. LanguageToggle is already "use client". Logo is a server component with inline SVG. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Nav to client component with scroll-aware glassmorphism</name>
  <files>src/components/Nav/Nav.tsx</files>
  <action>
Convert Nav.tsx from a server component to a client component that applies glassmorphism blur when scrolled.

1. Add `"use client"` directive at the top of the file.

2. Import `useState` and `useEffect` from React.

3. Add scroll detection logic:
   - State: `const [scrolled, setScrolled] = useState(false)`
   - useEffect that adds a scroll event listener on mount
   - Set `scrolled = true` when `window.scrollY > 10` (small threshold so blur activates almost immediately after any scroll)
   - Use `{ passive: true }` on the scroll listener for performance
   - Clean up listener on unmount

4. Apply dynamic className on the `<nav>` element:
   - When NOT scrolled (at top): `bg-ink-deep border-b border-border` (current solid background, opaque)
   - When scrolled: `bg-ink-deep/80 backdrop-blur-md border-b border-border` (translucent background + glassmorphism blur)
   - Add `transition-all duration-300` for smooth visual transition between states

5. The rest of the component stays identical -- Logo and LanguageToggle imports and rendering unchanged.

6. DSGN-03 compliance: This adds 1 conditional backdrop-blur element. Existing count is 5 (4 feature cards + 1 DataFlow card). Total = 6 when scrolled, which equals the limit. The nav blur is only active when scrolled past the hero, so it will rarely share viewport with all 4 feature cards simultaneously. This is compliant.

Do NOT use IntersectionObserver -- a simple scroll listener with passive flag is sufficient and more straightforward for this threshold-based behavior.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build && npm run lint</automated>
  </verify>
  <done>Nav.tsx is a client component that renders solid bg-ink-deep at scroll position 0 and transitions to bg-ink-deep/80 + backdrop-blur-md when scrolled past 10px. Build and lint pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors (client component compiles correctly)
2. `npm run lint` passes
3. Nav.tsx contains `"use client"` directive
4. Nav.tsx contains `addEventListener("scroll"` with `passive: true`
5. Nav.tsx contains `backdrop-blur-md` in scrolled state
6. Nav.tsx contains `transition` classes for smooth effect
</verification>

<success_criteria>
- Nav shows solid dark background when at top of page
- Nav transitions to glassmorphism blur (translucent bg + backdrop-blur) when user scrolls down
- Nav transitions back to solid when user scrolls back to top
- Transition is smooth (300ms)
- Total backdrop-blur elements on page remains at 6 or fewer (DSGN-03)
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-layout/02-04-SUMMARY.md`
</output>
