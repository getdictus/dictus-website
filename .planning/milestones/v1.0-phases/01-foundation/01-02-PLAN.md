---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/i18n/routing.ts
  - src/i18n/navigation.ts
  - src/i18n/request.ts
  - src/proxy.ts
  - src/messages/fr.json
  - src/messages/en.json
  - src/components/Nav/Nav.tsx
  - src/components/Nav/Logo.tsx
  - src/components/Nav/LanguageToggle.tsx
autonomous: true
requirements:
  - NAV-01
  - I18N-01
  - I18N-02

must_haves:
  truths:
    - "Visiting /fr renders the page in French with French nav content"
    - "Visiting /en renders the page in English with English nav content"
    - "Visiting / redirects to /fr (default locale)"
    - "Browser with Accept-Language: en gets redirected to /en on first visit"
    - "User sees a sticky nav header with the dictus waveform logo + wordmark"
    - "User can click FR / EN toggle to switch language and URL updates"
  artifacts:
    - path: "src/proxy.ts"
      provides: "next-intl locale middleware (proxy.ts for Next.js 16)"
      exports: ["proxy", "config"]
    - path: "src/i18n/routing.ts"
      provides: "Locale routing config (fr, en, defaultLocale: fr, localePrefix: always)"
      exports: ["routing"]
    - path: "src/i18n/request.ts"
      provides: "Request config for next-intl server"
    - path: "src/components/Nav/Nav.tsx"
      provides: "Sticky navigation header component"
    - path: "src/components/Nav/Logo.tsx"
      provides: "Squircle waveform icon + dictus wordmark"
    - path: "src/components/Nav/LanguageToggle.tsx"
      provides: "FR / EN language switcher"
    - path: "src/messages/fr.json"
      provides: "French translations"
    - path: "src/messages/en.json"
      provides: "English translations"
  key_links:
    - from: "src/proxy.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware.*routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/Nav/Nav.tsx"
      via: "Nav component rendered in layout"
      pattern: "<Nav"
    - from: "src/components/Nav/LanguageToggle.tsx"
      to: "src/i18n/navigation.ts"
      via: "useRouter or Link from createNavigation"
      pattern: "useRouter|usePathname"
    - from: "src/app/[locale]/layout.tsx"
      to: "next-intl"
      via: "NextIntlClientProvider wrapping children"
      pattern: "NextIntlClientProvider"
---

<objective>
Wire up i18n routing (FR/EN) via next-intl v4 with browser locale detection, and build the sticky nav shell with logo and language toggle.

Purpose: Make the site bilingual from the start and give users the persistent navigation frame that all content sections will live under.
Output: Visiting /fr and /en shows the correct language, nav with dictus logo and FR/EN toggle is visible and functional.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- From Plan 01 -- executor needs these to wire i18n into existing layout -->

From src/app/[locale]/layout.tsx (created in Plan 01):
- Root layout with DM_Sans + DM_Mono font variables on html element
- Body with className="bg-ink font-sans text-white antialiased"
- generateStaticParams returning ['fr', 'en']
- Async params: Promise<{locale: string}>
- Needs: NextIntlClientProvider wrapper, Nav component import

From src/app/globals.css (created in Plan 01):
- @theme with all brand tokens (accent, accent-hi, border, etc.)
- @theme inline with font-sans and font-mono mappings

From next.config.ts (created in Plan 01):
- Already configured with createNextIntlPlugin from 'next-intl/plugin'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Set up next-intl v4 i18n routing with proxy.ts and translation files</name>
  <files>src/i18n/routing.ts, src/i18n/navigation.ts, src/i18n/request.ts, src/proxy.ts, src/messages/fr.json, src/messages/en.json, src/app/[locale]/layout.tsx</files>
  <action>
1. Create `src/i18n/routing.ts`:
   ```typescript
   import { defineRouting } from 'next-intl/routing';
   export const routing = defineRouting({
     locales: ['fr', 'en'],
     defaultLocale: 'fr',
     localePrefix: 'always',
   });
   ```

2. Create `src/i18n/navigation.ts`:
   ```typescript
   import { createNavigation } from 'next-intl/navigation';
   import { routing } from './routing';
   export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
   ```

3. Create `src/i18n/request.ts`:
   ```typescript
   import { getRequestConfig } from 'next-intl/server';
   import { hasLocale } from 'next-intl';
   import { routing } from './routing';

   export default getRequestConfig(async ({ requestLocale }) => {
     const requested = await requestLocale;
     const locale = hasLocale(routing.locales, requested)
       ? requested
       : routing.defaultLocale;
     return {
       locale,
       messages: (await import(`../messages/${locale}.json`)).default,
     };
   });
   ```

4. Create `src/proxy.ts` (NOT middleware.ts -- Next.js 16 convention):
   ```typescript
   import type { NextRequest } from 'next/server';
   import createMiddleware from 'next-intl/middleware';
   import { routing } from './i18n/routing';

   const intlProxy = createMiddleware(routing);

   export function proxy(request: NextRequest) {
     return intlProxy(request);
   }

   export const config = {
     matcher: '/((?!api|trpc|_next|_vercel|.*\\..*).*)',
   };
   ```

5. Create `src/messages/fr.json`:
   - Namespaced structure with "Nav" and "Metadata" sections
   - Nav: minimal (just language labels for now)
   - Metadata: title "dictus -- Dictation vocale 100% offline pour iOS", description about offline voice dictation keyboard

6. Create `src/messages/en.json`:
   - Same structure as fr.json, English translations
   - Metadata: title "dictus -- 100% Offline Voice Dictation for iOS"

7. Update `src/app/[locale]/layout.tsx` (from Plan 01):
   - Add NextIntlClientProvider import from 'next-intl'
   - Add getMessages, setRequestLocale imports from 'next-intl/server'
   - Add hasLocale import from 'next-intl'
   - Add routing import from '@/i18n/routing'
   - In the async function: validate locale with hasLocale, call setRequestLocale(locale), await getMessages()
   - Wrap children with `<NextIntlClientProvider messages={messages}>`
   - Call notFound() if locale is invalid
   - Update metadata to use next-intl for locale-aware metadata (or keep static for now)

IMPORTANT: File is `src/proxy.ts`, NOT `src/middleware.ts`. Export a `proxy` function, NOT `middleware`.

IMPORTANT: Use `await params` for accessing locale (Next.js 16 async params).

IMPORTANT: localePrefix must be 'always' per user decision -- both /fr and /en are always prefixed, root / redirects to /fr.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Build succeeds with i18n routing configured
    - proxy.ts exists (not middleware.ts)
    - /fr and /en routes are generated in build output
    - Translation files exist for both locales with Nav and Metadata namespaces
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Nav shell with Logo and LanguageToggle components</name>
  <files>src/components/Nav/Nav.tsx, src/components/Nav/Logo.tsx, src/components/Nav/LanguageToggle.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx</files>
  <action>
1. Create `src/components/Nav/Logo.tsx`:
   - Inline SVG of the dictus waveform icon (same design as favicon): squircle with 3 asymmetric bars
   - Size: ~32px height for nav context
   - Next to the icon: "dictus" wordmark text in DM Sans 200 (font-extralight), letter-spacing -0.04em, lowercase
   - Wrap in a link to "/" (or current locale root)
   - Use the accent gradient on the waveform bars (#6BA3FF to #2563EB)
   - Icon background: gradient from #0D2040 to #071020

2. Create `src/components/Nav/LanguageToggle.tsx`:
   - Client component ('use client')
   - Display "FR" and "EN" as text buttons separated by " / "
   - Active language highlighted with accent blue (text-accent), inactive in white-40
   - On click: use `useRouter` and `usePathname` from `@/i18n/navigation` to switch locale
   - Use `useLocale` from 'next-intl' to determine active locale
   - Pattern: `router.replace(pathname, { locale: targetLocale })`

3. Create `src/components/Nav/Nav.tsx`:
   - Sticky nav: `fixed top-0 left-0 right-0 z-50`
   - Flex layout: Logo left, LanguageToggle far right
   - Background: bg-ink-deep (solid for now -- glassmorphism blur on scroll added in Phase 2)
   - Bottom border: border-b border-border (subtle rgba white 7%)
   - Padding: reasonable nav padding (Claude's discretion, ~px-6 py-4)
   - No section links, no CTA button (per user decision)

4. Update `src/app/[locale]/layout.tsx`:
   - Import Nav component
   - Render `<Nav />` before `{children}` inside the NextIntlClientProvider
   - Add padding-top to main content area to account for fixed nav height

5. Update `src/app/[locale]/page.tsx`:
   - Use `useTranslations` or `getTranslations` to display a localized placeholder
   - Show "dictus" wordmark centered, with a brief tagline from translations
   - This is a placeholder page -- Phase 2 will build the real hero and content sections
   - Demonstrate that translations work correctly in both /fr and /en

IMPORTANT: Nav is minimal per user decision -- logo left, language toggle far right, nothing else.

IMPORTANT: Logo wordmark is always lowercase "dictus", DM Sans 200 (font-extralight), letter-spacing -0.04em.

IMPORTANT: LanguageToggle shows "FR / EN" text buttons with active lang in accent blue. No dropdown, no flags.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Nav renders at top of page with logo left and language toggle right
    - Clicking FR/EN switches the URL between /fr and /en
    - Page content updates to match selected language
    - Logo shows squircle waveform icon + "dictus" wordmark
    - Nav is sticky (stays visible on scroll)
    - Active language is highlighted in accent blue
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npm run dev` -- visit http://localhost:3000/ -- redirects to /fr
3. Visit /fr -- page renders in French with nav showing logo and "FR / EN" toggle (FR highlighted)
4. Visit /en -- page renders in English with nav showing logo and "FR / EN" toggle (EN highlighted)
5. Click "EN" on /fr page -- URL changes to /en, content switches to English
6. Click "FR" on /en page -- URL changes to /fr, content switches to French
7. `curl -s -o /dev/null -w "%{redirect_url}" -H "Accept-Language: en" http://localhost:3000/` -- shows redirect to /en
8. Nav stays fixed at top when scrolling
9. No external network requests in browser Network tab (fonts self-hosted)
</verification>

<success_criteria>
- /fr and /en render correct locale content with proper URL structure
- Root / redirects to /fr (default locale)
- Browser locale detection redirects first-time visitors to matching language
- Sticky nav visible with dictus waveform logo + wordmark on the left, FR/EN toggle on the right
- Language toggle switches locale and URL, active language highlighted in accent blue
- Build succeeds, no console errors, no external requests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
