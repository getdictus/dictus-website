---
phase: 09-remotion-demo-video
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - video/src/components/MessagesScene.tsx
  - video/src/components/NotesScene.tsx
  - video/src/components/TextReveal.tsx
  - video/src/compositions/DictusDemo.tsx
  - video/src/Root.tsx
autonomous: true
requirements:
  - VID-03
  - VID-04

must_haves:
  truths:
    - "Messages scene shows iMessage-style conversation with dictus keyboard dictating a short phrase"
    - "Notes scene shows Apple Notes-style app with dictus keyboard dictating longer text"
    - "Horizontal slide transition connects Messages to Notes scene"
    - "Demo flow cycles: idle -> recording -> transcribing -> inserted for each scene"
    - "Video loops seamlessly from end back to beginning"
    - "All visible text is localized via locale prop"
  artifacts:
    - path: "video/src/compositions/DictusDemo.tsx"
      provides: "Main composition orchestrating scenes and state machine"
      contains: "Sequence"
    - path: "video/src/components/MessagesScene.tsx"
      provides: "iMessage-style scene"
      contains: "contactName"
    - path: "video/src/components/NotesScene.tsx"
      provides: "Apple Notes-style scene"
      contains: "notes"
    - path: "video/src/components/TextReveal.tsx"
      provides: "Text appearance animation"
      contains: "interpolate"
  key_links:
    - from: "video/src/compositions/DictusDemo.tsx"
      to: "video/src/components/MessagesScene.tsx"
      via: "Sequence component"
      pattern: "Sequence.*MessagesScene"
    - from: "video/src/compositions/DictusDemo.tsx"
      to: "video/src/components/NotesScene.tsx"
      via: "Sequence component"
      pattern: "Sequence.*NotesScene"
    - from: "video/src/Root.tsx"
      to: "video/src/compositions/DictusDemo.tsx"
      via: "Composition component reference"
      pattern: "DictusDemo"
---

<objective>
Build the two demo scenes (Messages and Notes), the text reveal animation, and the main DictusDemo composition that orchestrates everything with scene transitions and seamless looping.

Purpose: This is where the video comes together. VID-03 requires iPhone mockup scenes showing the full demo flow. VID-04 requires iOS-native animations and transitions. The composition must render two complete localized videos.
Output: Complete DictusDemo composition viewable in Remotion Studio with both FR and EN variants.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-remotion-demo-video/09-CONTEXT.md
@.planning/phases/09-remotion-demo-video/09-RESEARCH.md
@.planning/phases/09-remotion-demo-video/09-01-SUMMARY.md
@.planning/phases/09-remotion-demo-video/09-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 lib modules -->

From video/src/lib/i18n.ts:
```typescript
export type Locale = "fr" | "en";
export interface VideoContent {
  messages: { contactName: string; existingMessages: Array<{ from: "contact" | "user"; text: string }>; dictatedPhrase: string };
  notes: { title: string; existingText: string; dictatedText: string };
  captions: { recording: string; transcribing: string; inserted: string };
  tagline: string;
}
export const CONTENT: Record<Locale, VideoContent>;
```

From video/src/lib/spring-configs.ts:
```typescript
export const iosSpring = { standard, bouncy, gentle, snappy };
export const STATE_DURATIONS = { idle: 40, recording: 70, transcribing: 55, inserted: 45 };
export const SCENE_CYCLE_FRAMES = 210; // 7s per scene
export const SCENE_TRANSITION_FRAMES = 20; // ~0.67s
```

From video/src/lib/waveform-math.ts:
```typescript
export type DemoState = "idle" | "recording" | "transcribing" | "inserted";
```

From video/src/lib/colors.ts:
```typescript
export const COLORS = { inkDeep, surface, white, white70, white40, white30, accentBlue, ... };
export const STATE_COLORS = { idle, recording, transcribing, inserted };
```

<!-- From Plan 02 components -->

From video/src/components/IPhoneMockup.tsx:
```typescript
export const IPhoneMockup: React.FC<{ variant: "realistic" | "screen-only"; children: React.ReactNode; width?: number }>;
```

From video/src/components/DictusKeyboard.tsx:
```typescript
export const DictusKeyboard: React.FC<{ state: DemoState; stateStartFrame: number; width: number }>;
```

From video/src/components/StateIndicator.tsx:
```typescript
export const StateIndicator: React.FC<{ state: DemoState; label: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TextReveal, MessagesScene, and NotesScene components</name>
  <files>video/src/components/TextReveal.tsx, video/src/components/MessagesScene.tsx, video/src/components/NotesScene.tsx</files>
  <read_first>
    - src/components/Hero/TextReveal.tsx (visual reference for text appearance animation)
    - video/src/lib/i18n.ts (CONTENT structure for messages and notes)
    - video/src/lib/colors.ts (brand colors)
    - video/src/lib/spring-configs.ts (STATE_DURATIONS, iosSpring)
    - video/src/components/DictusKeyboard.tsx (import for keyboard)
    - video/src/components/StateIndicator.tsx (import for state pill)
    - video/src/components/IPhoneMockup.tsx (import for device frame)
  </read_first>
  <action>
1. Create `video/src/components/TextReveal.tsx`:
   - Props: `{ text: string; startFrame: number; durationFrames?: number }`
   - Uses `useCurrentFrame()` from remotion
   - Text appears with a spring-driven opacity + translateY animation
   - Characters or words reveal progressively using `interpolate()`:
     - Map frame range [startFrame, startFrame + durationFrames] to opacity [0, 1] and translateY [8, 0]
     - Use `spring()` with `iosSpring.gentle` config for smooth appearance
   - Text color: COLORS.white, font: DM Sans 300

2. Create `video/src/components/MessagesScene.tsx`:
   - Props: `{ locale: Locale; demoState: DemoState; stateStartFrame: number }`
   - Full-screen layout (fills IPhoneMockup screen area):
     a. **App header** (top): Blue background (iOS Messages blue #007AFF or similar), white title showing contact name from CONTENT[locale].messages.contactName, back arrow icon on left
     b. **Message list** (middle, flex-grow):
        - Existing messages from CONTENT[locale].messages.existingMessages rendered as chat bubbles
        - Contact messages: left-aligned, gray background (#3A3A3C), white text
        - User messages: right-aligned, blue background (#007AFF), white text
        - Bubbles have iOS-style rounded corners (18px radius, with tail shape optional)
        - When demoState is "inserted": the dictated phrase appears as a new user bubble at bottom using TextReveal animation
     c. **Keyboard area** (bottom, fixed height ~260px): Renders `<DictusKeyboard>` component with state and stateStartFrame
   - Background: dark (#1C1C1E for iOS Messages dark mode feel)

3. Create `video/src/components/NotesScene.tsx`:
   - Props: `{ locale: Locale; demoState: DemoState; stateStartFrame: number }`
   - Full-screen layout:
     a. **App header** (top): Notes-style header with CONTENT[locale].notes.title, yellow/orange folder icon, "Done" button on right, DM Sans 400
     b. **Note content** (middle, flex-grow):
        - Yellow-ish title area or plain white text area (Apple Notes dark mode: #1C1C1E background)
        - When demoState is "inserted": the dictated text appears using TextReveal animation
        - Text in DM Sans 300, COLORS.white, line-height 1.6
     c. **Keyboard area** (bottom, fixed height ~260px): Renders `<DictusKeyboard>` component
   - Background: #1C1C1E (iOS Notes dark mode)
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website/video && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - video/src/components/TextReveal.tsx contains `useCurrentFrame` from remotion
    - video/src/components/TextReveal.tsx contains `interpolate` or `spring` from remotion
    - video/src/components/MessagesScene.tsx contains `CONTENT` import from i18n
    - video/src/components/MessagesScene.tsx contains `DictusKeyboard` import
    - video/src/components/MessagesScene.tsx contains `contactName` usage
    - video/src/components/MessagesScene.tsx contains `dictatedPhrase` usage
    - video/src/components/NotesScene.tsx contains `CONTENT` import from i18n
    - video/src/components/NotesScene.tsx contains `DictusKeyboard` import
    - video/src/components/NotesScene.tsx contains `dictatedText` usage
    - `cd video && npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Both scenes render complete app UIs with dictus keyboard and text reveal animation. Messages shows iMessage-style conversation. Notes shows Apple Notes-style editor. Both use localized content from i18n module.</done>
</task>

<task type="auto">
  <name>Task 2: Create DictusDemo composition and wire Root.tsx</name>
  <files>video/src/compositions/DictusDemo.tsx, video/src/Root.tsx</files>
  <read_first>
    - video/src/Root.tsx (current placeholder to replace with real DictusDemo import)
    - video/src/lib/spring-configs.ts (STATE_DURATIONS, SCENE_CYCLE_FRAMES, SCENE_TRANSITION_FRAMES)
    - video/src/lib/waveform-math.ts (DemoState type)
    - video/src/lib/i18n.ts (Locale type, CONTENT)
    - video/src/lib/colors.ts (COLORS for background)
    - video/src/components/IPhoneMockup.tsx (wrap scenes in mockup)
    - video/src/components/MessagesScene.tsx (Scene 1)
    - video/src/components/NotesScene.tsx (Scene 2)
    - video/src/components/StateIndicator.tsx (state label below phone)
    - .planning/phases/09-remotion-demo-video/09-CONTEXT.md (seamless loop, horizontal swipe transition, two mockup variants)
  </read_first>
  <action>
1. Create `video/src/compositions/DictusDemo.tsx`:
   - Props: `{ locale: Locale; variant?: "realistic" | "screen-only" }` (variant defaults to "realistic")
   - Uses `useCurrentFrame()`, `useVideoConfig()`, `Sequence`, `AbsoluteFill`, `spring`, `interpolate` from remotion

   **Background:**
   - AbsoluteFill with COLORS.inkDeep (#0A1628)
   - Centered radial gradient glow behind the phone (CSS radial-gradient with COLORS.glowSoft)

   **State machine (frame-based):**
   - Compute current scene (0=Messages, 1=Notes) and current DemoState from frame number
   - Timeline structure:
     ```
     Scene 1 (Messages): [0, SCENE_CYCLE_FRAMES)
       - idle: [0, STATE_DURATIONS.idle)
       - recording: [40, 40+70)
       - transcribing: [110, 110+55)
       - inserted: [165, 165+45)
     Transition: [SCENE_CYCLE_FRAMES, SCENE_CYCLE_FRAMES + SCENE_TRANSITION_FRAMES)
     Scene 2 (Notes): [SCENE_CYCLE_FRAMES + SCENE_TRANSITION_FRAMES, 2*SCENE_CYCLE_FRAMES + SCENE_TRANSITION_FRAMES)
       - Same state pattern offset by scene start frame
     Loop transition: last ~20 frames fade to match frame 0 state
     ```
   - Total duration: 2 * SCENE_CYCLE_FRAMES + 2 * SCENE_TRANSITION_FRAMES = 2*210 + 2*20 = 460 frames (~15.3s)
   - Helper function `getStateAtFrame(frame, sceneStartFrame)` returns `{ state: DemoState, stateStartFrame: number }`

   **Scene composition:**
   - IPhoneMockup centered in viewport (vertically and horizontally), width ~375 logical px scaled to fit 1080px video width
   - Use `interpolate()` for horizontal slide transition between scenes:
     - During transition frames: Scene 1 slides left (translateX 0 -> -100%), Scene 2 slides in from right (translateX 100% -> 0%)
     - Use `iosSpring.standard` for the slide spring
   - Both scenes rendered simultaneously during transition, clipped within IPhoneMockup

   **State indicator:**
   - Positioned below the iPhone mockup (centered)
   - Shows current state label from CONTENT[locale].captions
   - Hidden during "idle" state

   **Seamless loop:**
   - Last 20 frames: cross-fade (opacity interpolation) from current state back to idle state appearance
   - Frame 0 must show idle state of Messages scene
   - Last frame must visually match frame 0 (both show idle Messages)

   **Optional branding:**
   - Small "Dictus" wordmark (DM Sans 200, letter-spacing -0.04em) positioned at bottom center, below state indicator
   - Subtle opacity (COLORS.white40)
   - Tagline from CONTENT[locale].tagline in small text below wordmark

2. Update `video/src/Root.tsx`:
   - Remove placeholder DictusDemo component
   - Import real DictusDemo from `./compositions/DictusDemo`
   - Update both Composition entries:
     - `dictus-demo-fr`: component=DictusDemo, durationInFrames=460, fps=30, width=1080, height=1920, defaultProps={ locale: "fr" as const }
     - `dictus-demo-en`: same but locale: "en" as const
   - Add two additional compositions for screen-only variant comparison:
     - `dictus-demo-fr-screenonly`: same as fr but defaultProps includes `variant: "screen-only" as const`
     - `dictus-demo-en-screenonly`: same as en but with screen-only variant
   - Total: 4 compositions (2 per locale x 2 variants)
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-website/video && npx tsc --noEmit && npx remotion compositions src/index.ts 2>&1 | grep -c "dictus-demo" | xargs -I{} test {} -ge 4 && echo "PASS: 4+ compositions" || echo "FAIL: less than 4 compositions"</automated>
  </verify>
  <acceptance_criteria>
    - video/src/compositions/DictusDemo.tsx contains `useCurrentFrame` from remotion
    - video/src/compositions/DictusDemo.tsx contains `Sequence` from remotion
    - video/src/compositions/DictusDemo.tsx contains `interpolate` from remotion
    - video/src/compositions/DictusDemo.tsx contains `MessagesScene` import
    - video/src/compositions/DictusDemo.tsx contains `NotesScene` import
    - video/src/compositions/DictusDemo.tsx contains `IPhoneMockup` import
    - video/src/compositions/DictusDemo.tsx contains `COLORS.inkDeep` or `"#0A1628"` for background
    - video/src/compositions/DictusDemo.tsx contains horizontal slide transition logic (translateX)
    - video/src/compositions/DictusDemo.tsx contains loop transition logic (cross-fade in last frames)
    - video/src/Root.tsx contains `dictus-demo-fr` and `dictus-demo-en` composition IDs
    - video/src/Root.tsx contains `dictus-demo-fr-screenonly` and `dictus-demo-en-screenonly` composition IDs
    - video/src/Root.tsx contains `durationInFrames` set to 460
    - video/src/Root.tsx imports DictusDemo from `./compositions/DictusDemo`
    - `cd video && npx remotion compositions src/index.ts` lists 4 compositions
  </acceptance_criteria>
  <done>DictusDemo orchestrates Messages and Notes scenes with horizontal slide transition, 4-state demo cycle per scene, seamless loop, and localized content. Root.tsx registers 4 compositions (2 locales x 2 mockup variants). Video is viewable in Remotion Studio.</done>
</task>

</tasks>

<verification>
- `cd video && npx tsc --noEmit` passes
- `cd video && npx remotion compositions src/index.ts` lists 4 compositions
- `cd video && npx remotion studio src/index.ts` opens and shows the demo (manual visual check)
- DictusDemo uses Sequence for scene orchestration, spring for transitions
- No smart mode (purple state) anywhere in composition
</verification>

<success_criteria>
- Complete demo video viewable in Remotion Studio with both FR and EN variants
- Messages scene shows iMessage UI with conversation and dictated phrase
- Notes scene shows Notes app UI with dictated text
- Horizontal slide transition between scenes feels iOS-native
- Video loops seamlessly (last frame matches first frame visually)
- Two mockup variants available for user comparison
</success_criteria>

<output>
After completion, create `.planning/phases/09-remotion-demo-video/09-03-SUMMARY.md`
</output>
